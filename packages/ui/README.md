# @tools/ui

Presentational design system for the tools family: CSS tokens + `ui-*` classes + React wrappers.

```js
import '@tools/ui/css'
import { Button, Card, Logo } from '@tools/ui'
```

Brand per app by overriding `--fab-bg` / `--fab-bg-hover`.
