# AVG Design System

> Machine-readable design system for AI agents. Package: `@aics/storybook-avg`.
> Self-contained library — consume this brand alone.

## Start here (agents)

- [AGENTS.md](./AGENTS.md) — **mandatory** implementation rules
- [README.md](./README.md) — human + agent overview

## MUST

1. Read `AGENTS.md` before implementing any unit.
2. Fetch `/api/index.json` then `/api/<type>/<name>.json`.
3. Copy **only** the path strings in that JSON’s `sourceFiles` (+ `specUrl`).
   Do **not** invent URLs or extensions (no `.css`, no `*.config.json`).
4. Implement from those files + `/api/tokens.json`.
5. Keep published BEM class names; call published `init*` factories.
6. Do **not** invent markup/CSS/behavior from Docs prose or Show code alone.
7. Absolute brand base (prefix for every relative `/api/...` path):
   `https://ppeou-gendigital.github.io/tools/aics/avg/`

## Registry

- [Index of every unit](/api/index.json)
- [Design tokens (DTCG)](/api/tokens.json)

## Browse by type

- [Tokens](/api/tokens/)
- [Components](/api/components/)

## Asset inventories

- [Icons (407 assets)](/api/assets/icons.json)

## Bulk download

- [Concatenated spec.md + manifests](/llms-full.txt)

## Source

- Package: @aics/storybook-avg@0.1.0
- Storybook: https://ppeou-gendigital.github.io/tools/aics/avg/


# Full dump

## component/accordion

---
type: component
name: accordion
subtype: pattern
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "2499:2626"
status: published
behavior: true
composes:
  - icon
tokensConsumed:
  - --color-text-primary
  - --color-text-secondary
  - --color-text-accent
  - --color-border-strong
  - --color-border-subtle
  - --color-border-focus
  - --color-canvas-subtle
  - --color-canvas-contrast
  - --color-bg-subtle
  - --color-disabled-text
  - --color-disabled-bg
  - --space-0
  - --space-1
  - --space-2
  - --space-3
  - --space-4
  - --space-5
  - --space-6
  - --border-radius-s
  - --border-radius-l
  - --border-width-xs
  - --border-width-s
  - --font-family-primary
  - --font-size-body-lg
  - --font-size-body-base
  - --font-size-body-sm
  - --font-size-h7
  - --lineheight-body-lg
  - --lineheight-body-base
  - --lineheight-body-sm
  - --lineheight-h7
  - --font-weight-regular
  - --font-weight-medium
  - --font-weight-semibold
---

# Accordion

Vertical stack of expand/collapse rows. Each row pairs a title (the
clickable header) with a body region that opens to reveal long-form
content — paragraph, bulleted list, inline learn-more link, image, or
video. Mirrors the canonical
[Web-ODS Shared Library → Pattern / Accordion](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=539-28425&m=dev)
board (`2499:2626`), which publishes `accordion / group`
(`2499:2627`), `accordion / item`, and `accordion / card`.

## Summary

The Accordion is a progressive-disclosure pattern for vertically
stacked Q&A-style content. The component renders as a single
`.c-accordion` group wrapping one or more `.c-accordion__item` rows.
Every item is a native `<details>` / `<summary>` pair; co-located
`accordion.js` adds height animation, optional single-open enforcement,
and `other brand:accordion:*` custom events. Appearance `card` maps to the
Figma `accordion / card` set (rounded `--color-bg-subtle` surfaces).

## Composes

Composes: icon (trailing chevron `arrows-navigation/simple-expand-more`,
optional leading icon via catalog key).

### Geometry inventory (icon slots)

Nested INSTANCE → `size` mapping (control-pad + inner icon; avoid
double-count). Figma control `2499:2681` (`Control buttons`).

| slot | outer px | pad | icon INSTANCE px | size= | frame= | wrapper pad? |
|---|---|---|---|---|---|---|
| `__item-chevron` | 24×24 | `--space-1` (2px) | 20×20 | `20` | none | yes (`padding: var(--space-1)` on `.c-accordion__item-chevron`) |
| `__item-leading` | 24×24 | none | 24×24 | `24` | none | no |

## Variant axes

| Axis | Values |
|---|---|
| `appearance` | `default` (divider rows) / `card` (rounded filled surfaces — Figma `accordion / card`) |
| `state` | `default` / `expanded` / `hover` / `focus` / `pressed` / `disabled` |
| `topDivider` | `true` / `false` (default) — group-level |
| `singleOpen` | `true` / `false` (default) — JS-only |
| `showDivider` | per-item bottom rule (default `true`; typically `false` under `card`) |
| `showIcon` | trailing chevron (default `true`) |
| `showLeadingIcon` | optional leading 24 px icon (default `false`) |
| `showBody` / `showList` / `showLink` / `showImage` / `showVideo` | content slots |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Items | Array | `items` | yes | `[]` | `{ id?, title, body?, linkLabel?, linkHref?, listItems?, imageSrc?, imageAlt?, videoSrc?, expanded?, disabled?, showDivider?, showIcon?, showLeadingIcon?, leadingIconName?, showBody?, showList?, showLink?, showImage?, showVideo?, state? }` |
| 2 | Appearance | Enum | `appearance` | no | `default` | `default` \| `card` |
| 3 | Top divider | Boolean | `topDivider` | no | `false` | |
| 4 | Single open | Boolean | `singleOpen` | no | `false` | |
| 5 | Group title | String | `groupTitle` | no | `null` | Optional `<h2>` |
| 6 | Name | String | `name` | no | `null` | Group id namespace |

## Tokens consumed

**Structural** — `--space-0`…`--space-6`, `--border-radius-s` /
`--border-radius-l`, `--border-width-xs` / `--border-width-s`.

**Typography** — `--font-family-primary`, `--font-weight-regular` /
`--font-weight-medium` / `--font-weight-semibold`, body-lg / body-base /
body-sm / h7 size + line-height pairs.

**Color** — `--color-text-primary` / `--secondary` / `--accent`,
`--color-border-strong` / `--subtle` / `--focus`,
`--color-canvas-subtle` / `--contrast`, `--color-bg-subtle`,
`--color-disabled-text` / `--bg`.

See themes/default/ for resolved values.

## Responsive behaviour

Intrinsically full-width; single-column at every breakpoint. Image /
video slots use `max-inline-size: 100%`. Placement on the page grid is
the consumer's responsibility.

## States

- **Default / Expanded** — persistent; expanded is `<details open>`.
- **Hover / Focus / Pressed** — CSS pseudo-classes; story freezes via `data-state`.
- **Disabled** — `data-disabled="true"`; JS short-circuits toggles.
- **Reduced motion** — skips height animation.

## Accessibility

- Native `<details>` / `<summary>` for keyboard, SR, and find-in-page.
- Chevron / leading icons are decorative (`aria-hidden`).
- Focus-visible outline uses `--color-border-focus`.
- Disabled items keep a readable label; summary uses `tabindex="-1"` when disabled.

## Design intent

Provide one shared progressive-disclosure pattern for FAQ / help /
terms content across default theme and first-party brands, with both the
classic divider list and the card surface from Figma.

## JavaScript API

### Init signature

| option | type | default | description |
|---|---|---|---|
| `singleOpen` | `boolean` | from `data-single-open` | Override single-open |
| `animate` | `boolean` | `true` | Height animation |
| `duration` | `number` | `200` | Animation ms |

### Instance shape

```ts
{
  destroy(): void;
  open(target: number | string): void;
  close(target: number | string): void;
  toggle(target: number | string): void;
  getOpen(): string[];
}
```

### Events

| name | detail | when fired |
|---|---|---|
| `other brand:accordion:toggle` | `{ id, open, source }` | Every state change |
| `other brand:accordion:opened` | `{ id, source }` | After open animation |
| `other brand:accordion:closed` | `{ id, source }` | After close animation |

### State attributes

| attribute | values | when applied |
|---|---|---|
| `data-state` | `hover` / `focus` / `pressed` | Story freezes |
| `data-disabled` | `true` | Disabled item |
| `data-single-open` | `true` | Group single-open |
| `data-animating` | `opening` / `closing` | During height transition |

### Keyboard

| key | behavior |
|---|---|
| `Enter` / `Space` | Toggle focused item |
| `Tab` | Move focus |

### SSR fallback

Without JS, native `<details>` still toggles. Height animation,
single-open, and custom events require `initAccordion` /
`initAccordions`.


### accordion.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "pattern",
  "librarySource": "brand",
  "name": "accordion",
  "status": "published",
  "summary": "Vertical stack of expand/collapse rows. Each row pairs a title (the clickable header) with a body region that opens to reveal long-form content — paragraph, bulleted list, inline learn-more link, image, or video. Mirrors the canonical [Web-ODS Shared Library → Pattern / Accordion](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=539-28425&m=dev) board (`2499:2626`), which publishes `accordion / group` (`2499:2627`), `accordion / item`, and `accordion / card`.",
  "specUrl": "/api/components/accordion/spec.md",
  "sourceFiles": {
    "spec": "/api/components/accordion/spec.md",
    "hbs": "/api/components/accordion/source/accordion.hbs",
    "scss": "/api/components/accordion/source/accordion.scss",
    "js": "/api/components/accordion/source/accordion.js"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "2499:2626",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=2499-2626&m=dev"
  },
  "rootClass": "c-accordion",
  "bemClasses": [
    "c-accordion"
  ],
  "properties": [
    {
      "name": "items",
      "fixtureKey": "items",
      "type": "Array",
      "required": true,
      "default": "[]",
      "description": "`{ id?, title, body?, linkLabel?, linkHref?, listItems?, imageSrc?, imageAlt?, videoSrc?, expanded?, disabled?, showDivider?, showIcon?, showLeadingIcon?, leadingIconName?, showBody?, showList?, showLink?, showImage?, showVideo?, state? }`",
      "figmaLabel": "Items"
    },
    {
      "name": "appearance",
      "fixtureKey": "appearance",
      "type": "Enum",
      "required": false,
      "default": "default",
      "description": "`default` \\",
      "figmaLabel": "Appearance"
    },
    {
      "name": "topDivider",
      "fixtureKey": "topDivider",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "",
      "figmaLabel": "Top divider"
    },
    {
      "name": "singleOpen",
      "fixtureKey": "singleOpen",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "",
      "figmaLabel": "Single open"
    },
    {
      "name": "groupTitle",
      "fixtureKey": "groupTitle",
      "type": "String",
      "required": false,
      "default": "null",
      "description": "Optional `<h2>`",
      "figmaLabel": "Group title"
    },
    {
      "name": "name",
      "fixtureKey": "name",
      "type": "String",
      "required": false,
      "default": "null",
      "description": "Group id namespace",
      "figmaLabel": "Name"
    }
  ],
  "variants": [
    {
      "axis": "appearance",
      "values": "`default` (divider rows) / `card` (rounded filled surfaces — Figma `accordion / card`)"
    },
    {
      "axis": "state",
      "values": "default` / `expanded` / `hover` / `focus` / `pressed` / `disabled"
    },
    {
      "axis": "topDivider",
      "values": "`true` / `false` (default) — group-level"
    },
    {
      "axis": "singleOpen",
      "values": "`true` / `false` (default) — JS-only"
    },
    {
      "axis": "showDivider",
      "values": "per-item bottom rule (default `true`; typically `false` under `card`)"
    },
    {
      "axis": "showIcon",
      "values": "trailing chevron (default `true`)"
    },
    {
      "axis": "showLeadingIcon",
      "values": "optional leading 24 px icon (default `false`)"
    },
    {
      "axis": "showBody` / `showList` / `showLink` / `showImage` / `showVideo",
      "values": "content slots"
    }
  ],
  "tokensConsumed": [
    "--color-text-primary",
    "--color-text-secondary",
    "--color-text-accent",
    "--color-border-strong",
    "--color-border-subtle",
    "--color-border-focus",
    "--color-canvas-subtle",
    "--color-canvas-contrast",
    "--color-bg-subtle",
    "--color-disabled-text",
    "--color-disabled-bg",
    "--space-0",
    "--space-1",
    "--space-2",
    "--space-3",
    "--space-4",
    "--space-5",
    "--space-6",
    "--border-radius-s",
    "--border-radius-l",
    "--border-width-xs",
    "--border-width-s",
    "--font-family-primary",
    "--font-size-body-lg",
    "--font-size-body-base",
    "--font-size-body-sm",
    "--font-size-h7",
    "--lineheight-body-lg",
    "--lineheight-body-base",
    "--lineheight-body-sm",
    "--lineheight-h7",
    "--font-weight-regular",
    "--font-weight-medium",
    "--font-weight-semibold"
  ],
  "composes": [
    "icon"
  ],
  "hasBehavior": true,
  "accessibility": [
    "Native `<details>` / `<summary>` for keyboard, SR, and find-in-page.",
    "Chevron / leading icons are decorative (`aria-hidden`).",
    "Focus-visible outline uses `--color-border-focus`.",
    "Disabled items keep a readable label; summary uses `tabindex=\"-1\"` when disabled."
  ],
  "storybookId": "patterns-accordion",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/award-wrapper

---
type: component
name: award-wrapper
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "3762:246"
status: published
composes: []
tokensConsumed:
  - --border-radius-0
  - --space-0
  - --color-disabled-bg
  - --color-text-inverse
  - --font-family-primary
  - --font-weight-regular
  - --font-size-label
  - --lineheight-label
  - --letterspacing-label
---

# Award Wrapper

Fixed-aspect frame for a single award logo so a row of awards lines up cleanly. Figma: [Molecule / Award Wrapper](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=3721-3904&m=dev) (variant board `3762:246`).

## Summary

Ratio-locked presentational box (transparent surface, square corners, clipped overflow) with a native `img` slot. When no logo is supplied, a documentation-only placeholder paints disabled-bg + inverse label text.

## Composes

Composes: none.

## Variant axes

| Axis | Values |
|---|---|
| Image ratio | `1:1`, `16:9`, `9:16` |

Fixed box sizes from Figma: `1:1` → 88×88 · `16:9` → 246×138 · `9:16` → 90×160.

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Image ratio | enum | `imageRatio` | yes | `1:1` | `1:1` \| `16:9` \| `9:16` → BEM `--ratio-*` |
| 2 | — | image-url | `src` | no | `""` | Award logo URL; empty shows placeholder |
| 3 | — | text | `alt` | no | `""` | Descriptive alt, or `""` when named nearby |
| 4 | — | text | `className` | no | `""` | Outer box escape hatch only |

## Tokens consumed

**Structural** — `--border-radius-0`, `--space-0`.

**Placeholder** — `--color-disabled-bg`, `--color-text-inverse`, `--font-family-primary`, `--font-weight-regular`, `--font-size-label`, `--lineheight-label`, `--letterspacing-label`.

See themes/default/ for resolved values.

## Responsive behaviour

Fixed pixel sizes per Image ratio; no breakpoint reflow. Parent layout owns spacing and wrapping of multiple instances.

## States

- Default (logo present) — transparent surface + `img` fill (`object-fit: contain`).
- Placeholder (no `src`) — disabled-bg fill + centred inverse label (docs / Storybook only).
- Non-interactive — no hover / focus / active paints; wrapping links own keyboard when linked.

## Accessibility

- Render logo as a native `img` with descriptive `alt` naming the award, or `alt=""` when the award is already named in adjacent text.
- No custom roles; meaning is carried by `alt`.
- Non-interactive — never in the tab order. If an award links out, the wrapping link owns keyboard + focus.
- Placeholder label is documentation-only and not shipped to users.

## Design intent

Use inside an Awards strip, footer trust row, or ratings block. Do not use for product / editorial / hero imagery (Media), non-award logos (Logo wrapper), or to lay out the whole row (compose several instances in a parent grid/flex).

## Overview

Use to frame a single award logo at a fixed aspect ratio so a row of awards lines up cleanly.

## When to use

- Image ratio `1:1` for square badges and seals, `16:9` for wide award lockups, and `9:16` for tall portrait marks.
- Inside an Awards strip, footer trust row, or ratings block where the parent owns spacing and wrapping.

## When not to use

- Product, editorial, or hero imagery — use the Media molecule.
- Brand or partner logos that are not awards — use the Logo wrapper.
- Laying out the whole award row — compose several Award wrapper instances inside a parent grid or flex row.

## Anatomy

1. **Surface** — ratio-locked box; transparent background; square corners; overflow clipped.
2. **Image slot** — award logo (`img`) filling the box.
3. **Placeholder** — when no logo, `--color-disabled-bg` fill with centred “Placeholder award logo” label in inverse content color (Label type ramp).


### award-wrapper.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "award-wrapper",
  "status": "published",
  "summary": "Fixed-aspect frame for a single award logo so a row of awards lines up cleanly. Figma: [Molecule / Award Wrapper](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=3721-3904&m=dev) (variant board `3762:246`).",
  "specUrl": "/api/components/award-wrapper/spec.md",
  "sourceFiles": {
    "spec": "/api/components/award-wrapper/spec.md",
    "hbs": "/api/components/award-wrapper/source/award-wrapper.hbs",
    "scss": "/api/components/award-wrapper/source/award-wrapper.scss"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "3762:246",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=3762-246&m=dev"
  },
  "rootClass": "c-award-wrapper",
  "bemClasses": [
    "c-award-wrapper"
  ],
  "properties": [
    {
      "name": "imageRatio",
      "fixtureKey": "imageRatio",
      "type": "enum",
      "required": true,
      "default": "1:1",
      "description": "`1:1` \\",
      "figmaLabel": "Image ratio"
    },
    {
      "name": "src",
      "fixtureKey": "src",
      "type": "image-url",
      "required": false,
      "default": "\"\"",
      "description": "Award logo URL; empty shows placeholder",
      "figmaLabel": "—"
    },
    {
      "name": "alt",
      "fixtureKey": "alt",
      "type": "text",
      "required": false,
      "default": "\"\"",
      "description": "Descriptive alt, or `\"\"` when named nearby",
      "figmaLabel": "—"
    },
    {
      "name": "className",
      "fixtureKey": "className",
      "type": "text",
      "required": false,
      "default": "\"\"",
      "description": "Outer box escape hatch only",
      "figmaLabel": "—"
    }
  ],
  "variants": [
    {
      "axis": "Image ratio",
      "values": "1:1`, `16:9`, `9:16"
    }
  ],
  "tokensConsumed": [
    "--border-radius-0",
    "--space-0",
    "--color-disabled-bg",
    "--color-text-inverse",
    "--font-family-primary",
    "--font-weight-regular",
    "--font-size-label",
    "--lineheight-label",
    "--letterspacing-label"
  ],
  "composes": [],
  "hasBehavior": false,
  "accessibility": [
    "Render logo as a native `img` with descriptive `alt` naming the award, or `alt=\"\"` when the award is already named in adjacent text.",
    "No custom roles; meaning is carried by `alt`.",
    "Non-interactive — never in the tab order. If an award links out, the wrapping link owns keyboard + focus.",
    "Placeholder label is documentation-only and not shipped to users."
  ],
  "storybookId": "molecules-award-wrapper",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/badge

---
type: component
name: badge
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "1284:14251"
status: published
composes: []
tokensConsumed:
  - --color-signal-info
  - --color-signal-info-muted
  - --color-signal-info-subtle
  - --color-signal-success
  - --color-signal-success-muted
  - --color-signal-success-subtle
  - --color-signal-warning
  - --color-signal-warning-muted
  - --color-signal-warning-subtle
  - --color-signal-critical
  - --color-signal-critical-muted
  - --color-signal-critical-subtle
  - --color-text-primary
  - --color-text-secondary
  - --color-text-inverse
  - --color-bg-subtle
  - --color-bg-inverse-strong
  - --color-bg-brand
  - --color-bg-brand-soft
  - --color-text-brand
  - --color-border-brand
  - --border-radius-pill
  - --space-1
  - --space-3
  - --font-family-primary
  - --font-weight-semibold
  - --font-size-body-xs
  - --lineheight-body-xs
---

# Badge

Compact molecule for status, counts, and short text labels. Canonical
core unit on BEM `.c-badge`, mirrored from
[Web-ODS Shared Library / page 539:28416](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=539-28416&m=dev)
(component set `1284:14251`, Spec `2959:441`). Brands inherit via
`onboarding` / `inherited-units.json` and supply theme tokens only.

## Summary

Three structural variants (`Dot` / `Count` / `Text`) × six colors
(Info / Success / Warning / Error / Brand / **Inverse**) × two
emphasis modes (Low / High) = **36** published variants. Paints bind
to the core theme contract (`--color-signal-*`, brand, inverse /
neutral surfaces) so White Label / other brand / other brand / AVG / other brand
re-skin without component forks.

## Composes

Composes: none.

## Variant axes

| Axis | Values |
|---|---|
| `variant` | `dot` (default — 8 × 8 px circle) / `count` (pill + `countValue`) / `text` (pill + uppercase `label`). BEM `.c-badge--<variant>`. |
| `color` | `info` / `success` / `warning` / `error` / `brand` / `inverse`. BEM `.c-badge--<color>`. Mirrors Figma Color on `1284:14251` (Inverse replaces the former other brand `gray` label). |
| `emphasis` | `low` (default) / `high`. BEM `.c-badge--<emphasis>`. |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Variant | VARIANT | `variant` | no | `"dot"` | `dot` / `count` / `text`. |
| 2 | Color | VARIANT | `color` | no | `"info"` | `info` / `success` / `warning` / `error` / `brand` / `inverse`. |
| 3 | Emphasis | VARIANT | `emphasis` | no | `"low"` | `low` / `high`. |
| 4 | Count value | TEXT | `countValue` | yes when `count` | `"3"` | Inside `.c-badge__value`. |
| 5 | Label | TEXT | `label` | yes when `text` | `"NEW"` | Inside `.c-badge__label`; SCSS uppercases. |
| — | — | String | `accessibleLabel` | no (`dot` only) | `""` | `role="img"` + `aria-label` when set; else `aria-hidden="true"`. |

## Tokens consumed

**Structural** — `count` / `text`: `border-radius: var(--border-radius-pill)`,
`padding: var(--space-1) var(--space-3)`. `dot`: 8 × 8 px, `border-radius: 50%`.

**Typography** — body-xs SemiBold (`--font-family-primary`,
`--font-weight-semibold`, `--font-size-body-xs`, `--lineheight-body-xs`).

**Per-color paint**

| Color | Low — Dot | High — Dot | Low — pill bg | Low — pill content | High — pill bg | High — pill content |
|---|---|---|---|---|---|---|
| `info` | `--color-signal-info-muted` | `--color-signal-info` | `--color-signal-info-subtle` | `--color-signal-info` | `--color-signal-info` | `--color-text-inverse` |
| `success` | `--color-signal-success-muted` | `--color-signal-success` | `--color-signal-success-subtle` | `--color-signal-success` | `--color-signal-success` | `--color-text-inverse` |
| `warning` | `--color-signal-warning-muted` | `--color-signal-warning` | `--color-signal-warning-subtle` | `--color-text-primary` | `--color-signal-warning` | `--color-text-primary` |
| `error` | `--color-signal-critical-muted` | `--color-signal-critical` | `--color-signal-critical-subtle` | `--color-signal-critical` | `--color-signal-critical` | `--color-text-inverse` |
| `brand` | `--color-border-brand` | `--color-bg-brand` | `--color-bg-brand-soft` | `--color-text-brand` | `--color-bg-brand` | `--color-text-inverse` |
| `inverse` | `--color-text-secondary` | `--color-bg-inverse-strong` | `--color-bg-subtle` | `--color-text-secondary` | `--color-bg-inverse-strong` | `--color-text-inverse` |

`warning` / `high` keeps dark text on yellow for WCAG contrast.

See brand `themes/default/` for resolved values against
@aics/storybook-avg.

## Responsive behaviour

Fit-content inline molecule. No breakpoint axis. Logical padding for RTL.

## States

| State | Mechanism | Notes |
|---|---|---|
| `default` | — | Presentation-only; no hover / focus / disabled paints or JS. |

## Accessibility

- **`dot`:** `aria-hidden="true"` by default; optional `accessibleLabel` → `role="img"`.
- **`count` / `text`:** visible text is the accessible name.
- **No focus ring** — wrap in an interactive control when needed.
- **Contrast:** `warning` / `high` carve-out; verify `inverse` / `low` on tinted hosts.

## Design intent

One BEM block for status dots, counts, and short marketing labels.
Stateless by design — compose with Button / Chip for interaction.
No size axis and no icon slot.

## Figma source

- Page: [`539:28416`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=539-28416&m=dev)
- Component set: `1284:14251`
- Spec: `2959:441`


### badge.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "badge",
  "status": "published",
  "summary": "Compact molecule for status, counts, and short text labels. Canonical core unit on BEM `.c-badge`, mirrored from [Web-ODS Shared Library / page 539:28416](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=539-28416&m=dev) (component set `1284:14251`, Spec `2959:441`). Brands inherit via `onboarding` / `inherited-units.json` and supply theme tokens only.",
  "specUrl": "/api/components/badge/spec.md",
  "sourceFiles": {
    "spec": "/api/components/badge/spec.md",
    "hbs": "/api/components/badge/source/badge.hbs",
    "scss": "/api/components/badge/source/badge.scss"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "1284:14251",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=1284-14251&m=dev"
  },
  "rootClass": "c-badge",
  "bemClasses": [
    "c-badge"
  ],
  "properties": [
    {
      "name": "variant",
      "fixtureKey": "variant",
      "type": "VARIANT",
      "required": false,
      "default": "\"dot\"",
      "description": "`dot` / `count` / `text`.",
      "figmaLabel": "Variant"
    },
    {
      "name": "color",
      "fixtureKey": "color",
      "type": "VARIANT",
      "required": false,
      "default": "\"info\"",
      "description": "`info` / `success` / `warning` / `error` / `brand` / `inverse`.",
      "figmaLabel": "Color"
    },
    {
      "name": "emphasis",
      "fixtureKey": "emphasis",
      "type": "VARIANT",
      "required": false,
      "default": "\"low\"",
      "description": "`low` / `high`.",
      "figmaLabel": "Emphasis"
    },
    {
      "name": "countValue",
      "fixtureKey": "countValue",
      "type": "TEXT",
      "required": false,
      "default": "\"3\"",
      "description": "Inside `.c-badge__value`.",
      "figmaLabel": "Count value"
    },
    {
      "name": "label",
      "fixtureKey": "label",
      "type": "TEXT",
      "required": false,
      "default": "\"NEW\"",
      "description": "Inside `.c-badge__label`; SCSS uppercases.",
      "figmaLabel": "Label"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "String",
      "required": false,
      "default": "\"\"",
      "description": "`role=\"img\"` + `aria-label` when set; else `aria-hidden=\"true\"`.",
      "figmaLabel": "—"
    }
  ],
  "variants": [
    {
      "axis": "variant",
      "values": "`dot` (default — 8 × 8 px circle) / `count` (pill + `countValue`) / `text` (pill + uppercase `label`). BEM `.c-badge--<variant>`."
    },
    {
      "axis": "color",
      "values": "`info` / `success` / `warning` / `error` / `brand` / `inverse`. BEM `.c-badge--<color>`. Mirrors Figma Color on `1284:14251` (Inverse replaces the former other brand `gray` label)."
    },
    {
      "axis": "emphasis",
      "values": "`low` (default) / `high`. BEM `.c-badge--<emphasis>`."
    }
  ],
  "tokensConsumed": [
    "--color-signal-info",
    "--color-signal-info-muted",
    "--color-signal-info-subtle",
    "--color-signal-success",
    "--color-signal-success-muted",
    "--color-signal-success-subtle",
    "--color-signal-warning",
    "--color-signal-warning-muted",
    "--color-signal-warning-subtle",
    "--color-signal-critical",
    "--color-signal-critical-muted",
    "--color-signal-critical-subtle",
    "--color-text-primary",
    "--color-text-secondary",
    "--color-text-inverse",
    "--color-bg-subtle",
    "--color-bg-inverse-strong",
    "--color-bg-brand",
    "--color-bg-brand-soft",
    "--color-text-brand",
    "--color-border-brand",
    "--border-radius-pill",
    "--space-1",
    "--space-3",
    "--font-family-primary",
    "--font-weight-semibold",
    "--font-size-body-xs",
    "--lineheight-body-xs"
  ],
  "composes": [],
  "hasBehavior": false,
  "accessibility": [
    "**`dot`:** `aria-hidden=\"true\"` by default; optional `accessibleLabel` → `role=\"img\"`.",
    "**`count` / `text`:** visible text is the accessible name.",
    "**No focus ring** — wrap in an interactive control when needed.",
    "**Contrast:** `warning` / `high` carve-out; verify `inverse` / `low` on tinted hosts."
  ],
  "storybookId": "molecules-badge",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/button

---
type: component
name: button
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "2434:14483"
status: published
composes:
  - icon
tokensConsumed:
  - --button-primary-bg-default
  - --button-primary-bg-hover
  - --button-primary-bg-pressed
  - --button-primary-content-default
  - --button-primary-content-hover
  - --button-primary-content-pressed
  - --button-primary-border
  - --button-secondary-bg-default
  - --button-secondary-bg-hover
  - --button-secondary-bg-pressed
  - --button-secondary-content-default
  - --button-secondary-content-hover
  - --button-secondary-content-pressed
  - --button-secondary-border
  - --button-tertiary-bg-default
  - --button-tertiary-bg-hover
  - --button-tertiary-bg-pressed
  - --button-tertiary-content-default
  - --button-tertiary-content-hover
  - --button-tertiary-content-pressed
  - --button-tertiary-border
  - --button-inverse-bg-default
  - --button-inverse-bg-hover
  - --button-inverse-bg-pressed
  - --button-inverse-content-default
  - --button-inverse-content-hover
  - --button-inverse-content-pressed
  - --button-inverse-border
  - --button-text-bg-default
  - --button-text-bg-hover
  - --button-text-bg-pressed
  - --button-text-content-default
  - --button-text-content-hover
  - --button-text-content-pressed
  - --button-primary-ghost-bg-default
  - --button-primary-ghost-bg-hover
  - --button-primary-ghost-bg-pressed
  - --button-primary-ghost-content-default
  - --button-primary-ghost-content-hover
  - --button-primary-ghost-content-pressed
  - --button-primary-ghost-border
  - --button-secondary-ghost-bg-default
  - --button-secondary-ghost-bg-hover
  - --button-secondary-ghost-bg-pressed
  - --button-secondary-ghost-content-default
  - --button-secondary-ghost-content-hover
  - --button-secondary-ghost-content-pressed
  - --button-secondary-ghost-border
  - --button-gradient-bg-default
  - --button-gradient-bg-hover
  - --button-gradient-bg-pressed
  - --button-gradient-content-default
  - --button-gradient-content-hover
  - --button-gradient-content-pressed
  - --button-gradient-border
  - --button-disabled-bg
  - --button-disabled-content
  - --button-disabled-border
  - --button-focus-ring
  - --button-spinner-track
  - --button-spinner-fill
  - --button-radius
  - --button-radius-focus
  - --button-border-width-primary
  - --button-border-width-secondary
  - --button-border-width-ghost
  - --button-border-width-focus
  - --button-block-size-s
  - --button-block-size-m
  - --button-block-size-l
  - --button-block-size-xl
  - --button-min-inline-size
  - --button-padding-block-s
  - --button-padding-block-m
  - --button-padding-block-l
  - --button-padding-block-xl
  - --button-padding-inline-s
  - --button-padding-inline-m
  - --button-padding-inline-l
  - --button-padding-inline-xl
  - --button-gap
  - --button-icon-size-s
  - --button-icon-size-m
  - --button-icon-size-l
  - --button-icon-size-xl
  - --font-family-primary
  - --font-size-body-base
  - --font-size-body-sm
  - --font-weight-semibold
  - --lineheight-body-base
  - --lineheight-body-sm
---

# Button

Canonical call-to-action — single BEM root (`.btn`) covering eight
`Type` styles × four `Size` values × `iconButton`. Mirrors
[Web-ODS Shared Library → Button `2434:14483`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=2434-14483&m=dev).
Layer-4 `--button-*` aliases live in `themes/default/_button.scss`
(default theme / brand themes); other brand keeps Figma-true paints in
`storybook-other brand/src/tokens/button/`.

## Summary

One `.btn` block with `--style` / `--size` / `--icon-button` modifiers.
Interaction states paint via CSS pseudo-classes. `loading` is
code-only (`is-loading` + spinner + `aria-busy`).

## Composes

Composes: icon (leading / trailing / icon-only slots via `{{> icon}}`).

## Variant axes

| Axis | Values |
|---|---|
| `style` | `primary` / `secondary` / `tertiary` / `inverse` / `text` / `primary-ghost` / `secondary-ghost` / `gradient` |
| `size` | `s` / `m` / `l` (default) / `xl` |
| `iconButton` | `false` (default) / `true` |
| `state` | `default` / `hover` / `focus` / `pressed` / `disabled` (CSS only) |
| `loading` | `false` (default) / `true` (code-only) |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Label | Text | `label` | when not iconButton | `Button` | |
| 2 | Type | Enum | `style` | no | `primary` | Figma Type axis |
| 3 | Size | Enum | `size` | no | `l` | |
| 4 | Icon button | Boolean | `iconButton` | no | `false` | Requires `accessibleLabel` |
| 5 | — | Boolean | `loading` | no | `false` | Code-only |
| 6 | — | Boolean | `disabled` | no | `false` | |
| 7 | — | Boolean | `showLeadingIcon` | no | `false` | |
| 8 | — | String | `leadingIcon` | no | — | Catalog key |
| 9 | — | Boolean | `showTrailingIcon` | no | `false` | |
| 10 | — | String | `trailingIcon` | no | — | Catalog key |
| 11 | — | String | `accessibleLabel` | when iconButton | `null` | |
| 12 | — | Enum | `type` | no | `button` | HTML type |

## Tokens consumed

**Surfaces** — `--button-{style}-*` per Type (see frontmatter).

**Geometry** — `--button-block-size-*`, padding / gap / icon-size ladder,
`--button-radius*`, `--button-border-width-*`.

**Universal** — `--button-disabled-*`, `--button-focus-ring`, spinner pair.

See `themes/default/_button.scss` for default theme values.

## Responsive behaviour

Fit-content molecule. No breakpoint axis — size is authored.

## States

- `hover` / `focus-visible` / `active` — CSS pseudo-classes (+ `data-state` freeze for galleries)
- `disabled` — native `disabled` + universal disabled paints
- `loading` — `.is-loading`, spinner replaces leading icon

## Accessibility

- Native `<button>`; `aria-label` required for icon-only
- Focus ring via `:focus-visible` + `--button-focus-ring`
- `aria-busy` when loading; honour `prefers-reduced-motion`

## Design intent

Shared CTA primitive for all first-party brands. Brand themes supply
`--button-*` paints; markup and BEM stay identical.


### button.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "button",
  "status": "published",
  "summary": "Canonical call-to-action — single BEM root (`.btn`) covering eight `Type` styles × four `Size` values × `iconButton`. Mirrors [Web-ODS Shared Library → Button `2434:14483`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=2434-14483&m=dev). Layer-4 `--button-*` aliases live in `themes/default/_button.scss` (default theme / brand themes); other brand keeps Figma-true paints in `storybook-other brand/src/tokens/button/`.",
  "specUrl": "/api/components/button/spec.md",
  "sourceFiles": {
    "spec": "/api/components/button/spec.md",
    "hbs": "/api/components/button/source/button.hbs",
    "scss": "/api/components/button/source/button.scss"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "2434:14483",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=2434-14483&m=dev"
  },
  "rootClass": "btn",
  "bemClasses": [
    "btn"
  ],
  "properties": [
    {
      "name": "label",
      "fixtureKey": "label",
      "type": "Text",
      "required": false,
      "default": "Button",
      "description": "",
      "figmaLabel": "Label"
    },
    {
      "name": "style",
      "fixtureKey": "style",
      "type": "Enum",
      "required": false,
      "default": "primary",
      "description": "Figma Type axis",
      "figmaLabel": "Type"
    },
    {
      "name": "size",
      "fixtureKey": "size",
      "type": "Enum",
      "required": false,
      "default": "l",
      "description": "",
      "figmaLabel": "Size"
    },
    {
      "name": "iconButton",
      "fixtureKey": "iconButton",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Requires `accessibleLabel`",
      "figmaLabel": "Icon button"
    },
    {
      "name": "loading",
      "fixtureKey": "loading",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Code-only",
      "figmaLabel": "—"
    },
    {
      "name": "disabled",
      "fixtureKey": "disabled",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "",
      "figmaLabel": "—"
    },
    {
      "name": "showLeadingIcon",
      "fixtureKey": "showLeadingIcon",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "",
      "figmaLabel": "—"
    },
    {
      "name": "leadingIcon",
      "fixtureKey": "leadingIcon",
      "type": "String",
      "required": false,
      "default": "—",
      "description": "Catalog key",
      "figmaLabel": "—"
    },
    {
      "name": "showTrailingIcon",
      "fixtureKey": "showTrailingIcon",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "",
      "figmaLabel": "—"
    },
    {
      "name": "trailingIcon",
      "fixtureKey": "trailingIcon",
      "type": "String",
      "required": false,
      "default": "—",
      "description": "Catalog key",
      "figmaLabel": "—"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "String",
      "required": false,
      "default": "null",
      "description": "",
      "figmaLabel": "—"
    },
    {
      "name": "type",
      "fixtureKey": "type",
      "type": "Enum",
      "required": false,
      "default": "button",
      "description": "HTML type",
      "figmaLabel": "—"
    }
  ],
  "variants": [
    {
      "axis": "style",
      "values": "primary` / `secondary` / `tertiary` / `inverse` / `text` / `primary-ghost` / `secondary-ghost` / `gradient"
    },
    {
      "axis": "size",
      "values": "s` / `m` / `l` (default) / `xl"
    },
    {
      "axis": "iconButton",
      "values": "false` (default) / `true"
    },
    {
      "axis": "state",
      "values": "`default` / `hover` / `focus` / `pressed` / `disabled` (CSS only)"
    },
    {
      "axis": "loading",
      "values": "`false` (default) / `true` (code-only)"
    }
  ],
  "tokensConsumed": [
    "--button-primary-bg-default",
    "--button-primary-bg-hover",
    "--button-primary-bg-pressed",
    "--button-primary-content-default",
    "--button-primary-content-hover",
    "--button-primary-content-pressed",
    "--button-primary-border",
    "--button-secondary-bg-default",
    "--button-secondary-bg-hover",
    "--button-secondary-bg-pressed",
    "--button-secondary-content-default",
    "--button-secondary-content-hover",
    "--button-secondary-content-pressed",
    "--button-secondary-border",
    "--button-tertiary-bg-default",
    "--button-tertiary-bg-hover",
    "--button-tertiary-bg-pressed",
    "--button-tertiary-content-default",
    "--button-tertiary-content-hover",
    "--button-tertiary-content-pressed",
    "--button-tertiary-border",
    "--button-inverse-bg-default",
    "--button-inverse-bg-hover",
    "--button-inverse-bg-pressed",
    "--button-inverse-content-default",
    "--button-inverse-content-hover",
    "--button-inverse-content-pressed",
    "--button-inverse-border",
    "--button-text-bg-default",
    "--button-text-bg-hover",
    "--button-text-bg-pressed",
    "--button-text-content-default",
    "--button-text-content-hover",
    "--button-text-content-pressed",
    "--button-primary-ghost-bg-default",
    "--button-primary-ghost-bg-hover",
    "--button-primary-ghost-bg-pressed",
    "--button-primary-ghost-content-default",
    "--button-primary-ghost-content-hover",
    "--button-primary-ghost-content-pressed",
    "--button-primary-ghost-border",
    "--button-secondary-ghost-bg-default",
    "--button-secondary-ghost-bg-hover",
    "--button-secondary-ghost-bg-pressed",
    "--button-secondary-ghost-content-default",
    "--button-secondary-ghost-content-hover",
    "--button-secondary-ghost-content-pressed",
    "--button-secondary-ghost-border",
    "--button-gradient-bg-default",
    "--button-gradient-bg-hover",
    "--button-gradient-bg-pressed",
    "--button-gradient-content-default",
    "--button-gradient-content-hover",
    "--button-gradient-content-pressed",
    "--button-gradient-border",
    "--button-disabled-bg",
    "--button-disabled-content",
    "--button-disabled-border",
    "--button-focus-ring",
    "--button-spinner-track",
    "--button-spinner-fill",
    "--button-radius",
    "--button-radius-focus",
    "--button-border-width-primary",
    "--button-border-width-secondary",
    "--button-border-width-ghost",
    "--button-border-width-focus",
    "--button-block-size-s",
    "--button-block-size-m",
    "--button-block-size-l",
    "--button-block-size-xl",
    "--button-min-inline-size",
    "--button-padding-block-s",
    "--button-padding-block-m",
    "--button-padding-block-l",
    "--button-padding-block-xl",
    "--button-padding-inline-s",
    "--button-padding-inline-m",
    "--button-padding-inline-l",
    "--button-padding-inline-xl",
    "--button-gap",
    "--button-icon-size-s",
    "--button-icon-size-m",
    "--button-icon-size-l",
    "--button-icon-size-xl",
    "--font-family-primary",
    "--font-size-body-base",
    "--font-size-body-sm",
    "--font-weight-semibold",
    "--lineheight-body-base",
    "--lineheight-body-sm"
  ],
  "composes": [
    "icon"
  ],
  "hasBehavior": false,
  "accessibility": [
    "Native `<button>`; `aria-label` required for icon-only",
    "Focus ring via `:focus-visible` + `--button-focus-ring`",
    "`aria-busy` when loading; honour `prefers-reduced-motion`"
  ],
  "storybookId": "molecules-button",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/checkbox

---
type: component
name: checkbox
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "665:36315"
status: published
behavior: true
composes:
  - icon
tokensConsumed:
  - --color-bg-default
  - --color-border-strong
  - --color-border-focus
  - --color-signal-info
  - --color-signal-success
  - --color-signal-critical
  - --color-text-primary
  - --color-text-secondary
  - --color-text-inverse
  - --color-disabled-bg
  - --color-disabled-border
  - --color-disabled-text
  - --space-1
  - --space-2
  - --space-3
  - --border-radius-control
  - --border-width-default
  - --border-width-emphasis
  - --font-family-primary
  - --font-size-body-base
  - --lineheight-body-base
  - --font-weight-regular
---

# Checkbox

Boolean / tri-state checkbox for multi-select forms. Mirrors the
canonical
[Web-ODS Shared Library → Checkbox master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=665-36315&m=dev)
(`665:36315`) and the per-tone hover / pressed background mapping at
[`665:36267`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=665-36267&m=dev).
Core default theme unit; brand packages supply theme token values.

## Summary

The Checkbox is a 24 × 24 px square box that progressively enhances
a native `<input type="checkbox">`. Three persistent states paint
the box: `unchecked` (hollow outline), `checked` (solid fill with
checkmark glyph), and `indeterminate` (solid fill with horizontal
dash). Three tone variants (`default` / `accent` / `critical`)
repaint the checked + indeterminate fills. Hover / focus / pressed
paint via a 32 × 32 px **state circle** that paints 4 px outside the
box on every side. The Figma source uses `rgba(80, 81, 101, 0.2)` —
the default theme `inverse-secondary` colour at 20 % opacity. other brand
has no dedicated state-wash alias today, so the implementation mixes
`--color-text-secondary` (= `--color-off-black` under other brand mode)
at 20 % / 30 % — a near-black wash that paints the same visual role
on the other brand surface palette (Designer Follow-Up logged for a
dedicated `--color-state-wash` alias). Linked Figma:
[`665:36315`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=665-36315&m=dev).

## Composes

Composes: `icon` — `status/simple-checkmark-small` paints when
`selection=checked`; `actions/simple-minus` paints when
`selection=indeterminate`. Both glyph wrappers are **always rendered**
in the markup and toggled via CSS `opacity` keyed on the `is-checked`
/ `is-indeterminate` modifier classes (mirrors Radio's `__dot`
pattern), so a JS click that flips `is-checked` doesn't have to
insert nodes. The icon element fills the 24 × 24 px box; the
`-small` SVG's path occupies ~50 % of its 24 px viewBox, so the
visible mark renders at ~12 px — matching the Figma master
`665:36315` "Extra" geometry.

## Variant axes

| Axis | Values |
|---|---|
| `tone` | `default` (info indigo on checked fill) / `accent` (success green) / `critical` (critical red). Tone affects the box fill in the `checked` and `indeterminate` states; in `unchecked` the box paints with the same neutral outline regardless of tone. |
| `state` (transient) | `default` / `hover` / `focused` / `pressed`. Pseudo-class driven on the native input. The state circle paints behind the box for all three transient paints. |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Tone | Enum (`default` / `accent` / `critical`) | `tone` | yes | `"default"` | Drives the checked + indeterminate fill. |
| 2 | Selection | Enum (`unchecked` / `checked` / `indeterminate`) | `selection` | no | `"unchecked"` | Persistent state. `checked` sets the native `checked` attribute; `indeterminate` sets the native `indeterminate` JS property (the `c-checkbox--indeterminate` modifier paints the dash). |
| 3 | Disabled | Boolean | `disabled` | no | `false` | Persistent. Paints the `--color-disabled-*` token trio, sets `aria-disabled="true"` on the root, sets `disabled` on the input. |
| 4 | Label | Text | `label` | no | `""` | Visible label rendered after the box (BEM element `c-checkbox__label`). Wrapped inside the root `<label>`. |
| 5 | Accessible label | Text | `accessibleLabel` | when no `label` is set | `""` | Bound to the input's `aria-label`. Required when no visible label is present. |
| 6 | Name | Text | `name` | no | `""` | Bound to the native `<input>` `name` attribute. |
| 7 | Value | Text | `value` | no | `"on"` | Bound to the native `<input>` `value` attribute. |

## Tokens consumed

**Structural** — `--border-radius-control` (4 px box corners), `--space-2` (4 px state-circle outset), `--space-3` (8 px label gap), `--border-width-default` (2 px box outline / focus ring), `--border-width-emphasis` (2 px box outline on checked / indeterminate).

**Surface** — `--color-bg-default` (unchecked box fill), `--color-border-strong` (unchecked box outline), `--color-signal-info` (checked + indeterminate fill, tone `default`), `--color-signal-success` (checked + indeterminate fill, tone `accent`), `--color-signal-critical` (checked + indeterminate fill, tone `critical`).

**State circle** — `--color-text-secondary` mixed at 20 % / 30 % opacity via `color-mix()` for the 32 × 32 px hover / focus / pressed wash. Substitutes for the default theme `inverse-secondary` source until other brand publishes a dedicated state-wash alias.

**Content** — `--color-text-primary` (visible label), `--color-text-inverse` (checkmark + dash glyph on the filled box), `--color-border-focus` (focus ring).

**Disabled** — `--color-disabled-bg` (box fill), `--color-disabled-border` (box outline), `--color-disabled-text` (glyph + label).

**Typography** — `--font-family-primary`, `--font-size-body-base`, `--lineheight-body-base`, `--font-weight-regular` (label).

## Responsive behaviour

The box paints at a fixed 24 × 24 px footprint across every
breakpoint (the state-circle ring extends the visual hit area to
32 × 32 px). The optional label flows inline after the box and
wraps if the container narrows; `flex-shrink: 0` on the box keeps
it at canonical size.

## States

- `unchecked` — hollow 2 px outline using `--color-border-strong`. Box fill is `--color-bg-default`.
- `checked` — solid fill using the tone color, white checkmark glyph centered.
- `indeterminate` — solid fill using the tone color, white horizontal dash centered. Visually replaces the checkmark.
- `hover` — paints the 32 × 32 px state circle (`--color-inverse-secondary` at 20% opacity) behind the box.
- `focused` — `:focus-visible` paints a 2 px focus ring on the state circle's 32 × 32 px area using `--color-border-focus`.
- `pressed` — `:active` paints the same state circle at slightly higher opacity (30%) for tactile feedback.
- `disabled` — paints the `--color-disabled-*` trio, sets `aria-disabled="true"`, sets native `disabled`. State circle does not paint.

The `[data-state]` hook on the root paints `hover` / `focused` /
`pressed` freezes for visual regression.

## Accessibility

- Uses native `<input type="checkbox">`; screen readers announce "Checked" / "Not checked" / "Mixed" (the last for `indeterminate`) directly.
- Always carries either a visible `<label>` (text after the box) or `aria-label` via `accessibleLabel`. Label is wrapped in the root `<label>` so clicking the text toggles the checkbox.
- Focus ring uses `:focus-visible` so it only appears on keyboard navigation.
- Keyboard: `Space` toggles. `Tab` / `Shift+Tab` move focus. The indeterminate visual is **input-only** — pressing `Space` while in indeterminate state transitions to either checked or unchecked (consumer's responsibility via JS).
- WCAG: every (tone × state) combination meets AA non-text contrast against `--color-bg-default`. Disabled paint retains a visible outline per WCAG 2.2 § 1.4.11.

## Design intent

Use a checkbox for multi-select boolean choices where the user can
pick zero or more (terms agreement, filter list, multi-select
options). For a binary setting that commits immediately, use a
Switch. For a single choice from a set, use a Radio.

## JavaScript API

### Init signature

| option | type | default | description |
|---|---|---|---|
| `commitOn` | `"input" \| "change"` | `"change"` | Which native event fires `other brand:checkbox:commit`. |

### Instance shape

```ts
type CheckboxInstance = {
  getSelection(): "unchecked" | "checked" | "indeterminate";
  setSelection(next: "unchecked" | "checked" | "indeterminate"): void;
  setDisabled(disabled: boolean): void;
  destroy(): void;
};
```

### Events

| name | detail | when fired |
|---|---|---|
| `other brand:checkbox:change` | `{ selection: "checked" \| "unchecked" }` | Native `change` on the underlying input. |
| `other brand:checkbox:commit` | `{ selection: "checked" \| "unchecked", source: "pointer" \| "keyboard" \| "api" }` | After commit per the `commitOn` option. |

### State attributes

| attribute | values | when applied |
|---|---|---|
| `data-component` | `"checkbox"` | Always on the root `<label>`. |
| `data-tone` | `"default" \| "accent" \| "critical"` | Always on the root. |
| `data-state` | `"hover" \| "focused" \| "pressed"` | Story-only freeze hook. |
| `aria-disabled` | `"true"` | On the root when `disabled=true`. |

### Keyboard

| key | behavior |
|---|---|
| `Space` | Toggle the checkbox. |
| `Tab` / `Shift+Tab` | Move focus. |

### SSR fallback

The native `<input type="checkbox">` ships in the template, so the
checkbox toggles fully without JS — pointer + keyboard both work.
The JS layer's only jobs are to dispatch the namespaced
`other brand:checkbox:*` CustomEvents and to set the native
`indeterminate` JS property when the modifier class is present
(SCSS can paint the visual but `indeterminate` is a JS-only
property on the input — without JS, screen readers will announce
"Not checked" rather than "Mixed").

## Notes & open questions

- **State-wash token substitution.** The Figma source for the
  hover / focus / pressed 32 × 32 px wash is the default theme
  `inverse-secondary` token at 20 % opacity (`rgba(80, 81, 101, 0.2)`).
  other brand has no `--color-inverse-secondary` alias; this unit
  substitutes `--color-text-secondary` (resolving to
  `--color-off-black` under the other brand mode pick) at 20 % / 30 %
  opacity via `color-mix()`. Designer follow-up: publish a dedicated
  `--color-state-wash` (or equivalent) semantic alias so this
  consumer doesn't bind to a text-role token. Same substitution is
  applied verbatim by `src/components/radio/checkbox.scss`.


### checkbox.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "checkbox",
  "status": "published",
  "summary": "Boolean / tri-state checkbox for multi-select forms. Mirrors the canonical [Web-ODS Shared Library → Checkbox master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=665-36315&m=dev) (`665:36315`) and the per-tone hover / pressed background mapping at [`665:36267`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=665-36267&m=dev). Core default theme unit; brand packages supply theme token values.",
  "specUrl": "/api/components/checkbox/spec.md",
  "sourceFiles": {
    "spec": "/api/components/checkbox/spec.md",
    "hbs": "/api/components/checkbox/source/checkbox.hbs",
    "scss": "/api/components/checkbox/source/checkbox.scss",
    "js": "/api/components/checkbox/source/checkbox.js"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "665:36315",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=665-36315&m=dev"
  },
  "rootClass": "c-icon",
  "bemClasses": [
    "c-icon",
    "c-checkbox"
  ],
  "properties": [
    {
      "name": "tone",
      "fixtureKey": "tone",
      "type": "Enum (`default` / `accent` / `critical`)",
      "required": true,
      "default": "\"default\"",
      "description": "Drives the checked + indeterminate fill.",
      "figmaLabel": "Tone"
    },
    {
      "name": "selection",
      "fixtureKey": "selection",
      "type": "Enum (`unchecked` / `checked` / `indeterminate`)",
      "required": false,
      "default": "\"unchecked\"",
      "description": "Persistent state. `checked` sets the native `checked` attribute; `indeterminate` sets the native `indeterminate` JS property (the `c-checkbox--indeterminate` modifier paints the dash).",
      "figmaLabel": "Selection"
    },
    {
      "name": "disabled",
      "fixtureKey": "disabled",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent. Paints the `--color-disabled-*` token trio, sets `aria-disabled=\"true\"` on the root, sets `disabled` on the input.",
      "figmaLabel": "Disabled"
    },
    {
      "name": "label",
      "fixtureKey": "label",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Visible label rendered after the box (BEM element `c-checkbox__label`). Wrapped inside the root `<label>`.",
      "figmaLabel": "Label"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Bound to the input's `aria-label`. Required when no visible label is present.",
      "figmaLabel": "Accessible label"
    },
    {
      "name": "name",
      "fixtureKey": "name",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Bound to the native `<input>` `name` attribute.",
      "figmaLabel": "Name"
    },
    {
      "name": "value",
      "fixtureKey": "value",
      "type": "Text",
      "required": false,
      "default": "\"on\"",
      "description": "Bound to the native `<input>` `value` attribute.",
      "figmaLabel": "Value"
    }
  ],
  "variants": [
    {
      "axis": "tone",
      "values": "`default` (info indigo on checked fill) / `accent` (success green) / `critical` (critical red). Tone affects the box fill in the `checked` and `indeterminate` states; in `unchecked` the box paints with the same neutral outline regardless of tone."
    },
    {
      "axis": "`state` (transient)",
      "values": "`default` / `hover` / `focused` / `pressed`. Pseudo-class driven on the native input. The state circle paints behind the box for all three transient paints."
    }
  ],
  "tokensConsumed": [
    "--color-bg-default",
    "--color-border-strong",
    "--color-border-focus",
    "--color-signal-info",
    "--color-signal-success",
    "--color-signal-critical",
    "--color-text-primary",
    "--color-text-secondary",
    "--color-text-inverse",
    "--color-disabled-bg",
    "--color-disabled-border",
    "--color-disabled-text",
    "--space-1",
    "--space-2",
    "--space-3",
    "--border-radius-control",
    "--border-width-default",
    "--border-width-emphasis",
    "--font-family-primary",
    "--font-size-body-base",
    "--lineheight-body-base",
    "--font-weight-regular"
  ],
  "composes": [
    "icon"
  ],
  "hasBehavior": true,
  "accessibility": [
    "Uses native `<input type=\"checkbox\">`; screen readers announce \"Checked\" / \"Not checked\" / \"Mixed\" (the last for `indeterminate`) directly.",
    "Always carries either a visible `<label>` (text after the box) or `aria-label` via `accessibleLabel`. Label is wrapped in the root `<label>` so clicking the text toggles the checkbox.",
    "Focus ring uses `:focus-visible` so it only appears on keyboard navigation.",
    "Keyboard: `Space` toggles. `Tab` / `Shift+Tab` move focus. The indeterminate visual is **input-only** — pressing `Space` while in indeterminate state transitions to either checked or unchecked (consumer's responsibility via JS).",
    "WCAG: every (tone × state) combination meets AA non-text contrast against `--color-bg-default`. Disabled paint retains a visible outline per WCAG 2.2 § 1.4.11."
  ],
  "storybookId": "molecules-checkbox",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/pagination-dots

---
type: component
name: pagination-dots
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "1278:1668"
status: published
composes: []
tokensConsumed:
  - --color-text-primary
  - --color-text-inverse
  - --color-bg-default
  - --color-border-subtle
  - --space-3
  - --border-radius-pill
  - --border-width-default
---

# Pagination Dots

Presentational pagination indicator: a horizontal row of small dots
where the active dot **expands** into a pill that announces the
current page. Mirrors the canonical
[Web-ODS Shared Library → Pagination dots master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=1278-1668&m=dev)
(`1278:1668`) and the
[light / dark preview composition](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=1278-1877&m=dev)
(`1278:1877`). Core default theme unit; brand packages supply theme token values.

## Summary

The component renders a row of `count` inactive dots (8 × 8 px) plus
**one** expanding active pill (~24 × 8 px) at position `current`.
Inactive dots use neutral border-subtle fills on a light surface
(`appearance: light`) or content-inverse fills on a dark surface
(`appearance: dark`). The active pill always paints with the inverse
of the inactive dot for contrast. The component is **presentational
only** — it does not own page-change navigation. Consumers wrap it in
their own navigation control (a list of links, a button group, a JS
carousel) and re-render with an updated `current` prop on page change.
Linked Figma:
[`1278:1668`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=1278-1668&m=dev).

## Composes

Composes: none. Dots are unstyled `<span>` elements painted via CSS.

## Variant axes

| Axis | Values |
|---|---|
| `count` | `2` … `8` integer. Total dot count. |
| `current` | `1` … `count` integer. Position of the active pill (1-indexed). |
| `appearance` | `light` (default) / `dark`. Repaints inactive vs active fills for the surface the component sits on. |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Count | Integer (2-8) | `count` | yes | `5` | Total number of dots rendered in the row. |
| 2 | Current | Integer (1-based) | `current` | yes | `1` | Position of the active expanding pill (1-indexed). Must be `>= 1` and `<= count`. |
| 3 | Appearance | Enum (`light` / `dark`) | `appearance` | no | `"light"` | `light` paints inactive dots `--color-border-subtle` + active pill `--color-text-primary`; `dark` paints inactive dots `--color-text-inverse` at 40 % opacity + active pill `--color-bg-default`. Designer follow-up: publish a dedicated `--color-pagination-dot-inactive-on-dark` semantic alias so the dark-mode wash doesn't bind to a foreground-role token at runtime opacity. |
| 4 | Accessible label | Text | `accessibleLabel` | no | `"Page {current} of {count}"` | Bound to the root `<nav>`'s `aria-label`. Tokens `{current}` and `{count}` interpolate at render time. |

## Tokens consumed

**Structural** — `--space-3` (8 px gap between dots — mirrors the Figma master `1278:1668` `gap: var(--space/space-3, 8px)`), `--border-radius-pill` (dot + pill corners), `--border-width-default` (focus ring when consumer wraps the component in an interactive control).

**Light appearance** — `--color-border-subtle` (inactive dot fill), `--color-text-primary` (active pill fill).

**Dark appearance** — `--color-text-inverse` (inactive dot fill at 40 % via `color-mix()`), `--color-bg-default` (active pill fill). Substitutes for a dedicated dark-surface inactive-dot semantic alias which other brand has not published; see properties table note.

## Responsive behaviour

The row paints at a fixed total inline size = `(count - 1) × 8px +
(count - 1) × 8px gap + active-pill-extra`. The active pill expands
inline; the surrounding dots do not reflow. RTL: gap + pill expansion
both use logical properties so the component reads natively under
`dir="rtl"`.

## States

This unit ships no interaction states. The semantic state is
captured in `aria-current="step"` on the active dot, and the root
`<nav>` announces the page position via `aria-label`.

## Accessibility

- Root element is `<nav>` with `aria-label="Page {current} of {count}"` (the literal interpolated label).
- Active dot carries `aria-current="step"`.
- Inactive dots are plain `<span>` (no interactive semantics).
- WCAG: every (appearance × position) combination meets AA non-text contrast on its target surface.

## Design intent

Use expanding-dot pagination for carousels, image galleries, and short
onboarding flows where total page count is small (≤ 8) and the visual
weight of a number-based pagination bar would dominate. For long page
counts use the canonical Pagination component (numbered).

## Notes & open questions

- **Dark-mode inactive-dot token substitution.** Same class of substitution as the Checkbox / Radio state-circle wash: the dark-mode inactive dots paint with `--color-text-inverse` at 40 % opacity via `color-mix()`, because other brand has no `--color-pagination-dot-inactive-on-dark` (or equivalent) semantic alias. Designer follow-up logged for a dedicated alias.


### pagination-dots.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "pagination-dots",
  "status": "published",
  "summary": "Presentational pagination indicator: a horizontal row of small dots where the active dot **expands** into a pill that announces the current page. Mirrors the canonical [Web-ODS Shared Library → Pagination dots master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=1278-1668&m=dev) (`1278:1668`) and the [light / dark preview composition](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=1278-1877&m=dev) (`1278:1877`). Core default theme unit; brand packages supply theme token values.",
  "specUrl": "/api/components/pagination-dots/spec.md",
  "sourceFiles": {
    "spec": "/api/components/pagination-dots/spec.md",
    "hbs": "/api/components/pagination-dots/source/pagination-dots.hbs",
    "scss": "/api/components/pagination-dots/source/pagination-dots.scss"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "1278:1668",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=1278-1668&m=dev"
  },
  "rootClass": "c-pagination-dots",
  "bemClasses": [
    "c-pagination-dots"
  ],
  "properties": [
    {
      "name": "count",
      "fixtureKey": "count",
      "type": "Integer (2-8)",
      "required": true,
      "default": "5",
      "description": "Total number of dots rendered in the row.",
      "figmaLabel": "Count"
    },
    {
      "name": "current",
      "fixtureKey": "current",
      "type": "Integer (1-based)",
      "required": true,
      "default": "1",
      "description": "Position of the active expanding pill (1-indexed). Must be `>= 1` and `<= count`.",
      "figmaLabel": "Current"
    },
    {
      "name": "appearance",
      "fixtureKey": "appearance",
      "type": "Enum (`light` / `dark`)",
      "required": false,
      "default": "\"light\"",
      "description": "`light` paints inactive dots `--color-border-subtle` + active pill `--color-text-primary`; `dark` paints inactive dots `--color-text-inverse` at 40 % opacity + active pill `--color-bg-default`. Designer follow-up: publish a dedicated `--color-pagination-dot-inactive-on-dark` semantic alias so the dark-mode wash doesn't bind to a foreground-role token at runtime opacity.",
      "figmaLabel": "Appearance"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "Text",
      "required": false,
      "default": "\"Page {current} of {count}\"",
      "description": "Bound to the root `<nav>`'s `aria-label`. Tokens `{current}` and `{count}` interpolate at render time.",
      "figmaLabel": "Accessible label"
    }
  ],
  "variants": [
    {
      "axis": "count",
      "values": "`2` … `8` integer. Total dot count."
    },
    {
      "axis": "current",
      "values": "`1` … `count` integer. Position of the active pill (1-indexed)."
    },
    {
      "axis": "appearance",
      "values": "`light` (default) / `dark`. Repaints inactive vs active fills for the surface the component sits on."
    }
  ],
  "tokensConsumed": [
    "--color-text-primary",
    "--color-text-inverse",
    "--color-bg-default",
    "--color-border-subtle",
    "--space-3",
    "--border-radius-pill",
    "--border-width-default"
  ],
  "composes": [],
  "hasBehavior": false,
  "accessibility": [
    "Root element is `<nav>` with `aria-label=\"Page {current} of {count}\"` (the literal interpolated label).",
    "Active dot carries `aria-current=\"step\"`.",
    "Inactive dots are plain `<span>` (no interactive semantics).",
    "WCAG: every (appearance × position) combination meets AA non-text contrast on its target surface."
  ],
  "storybookId": "molecules-pagination-dots",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/radio

---
type: component
name: radio
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "666:36462"
status: published
behavior: true
composes: []
tokensConsumed:
  - --color-bg-default
  - --color-border-strong
  - --color-border-focus
  - --color-signal-info
  - --color-signal-success
  - --color-signal-critical
  - --color-text-primary
  - --color-text-secondary
  - --color-text-inverse
  - --color-disabled-bg
  - --color-disabled-border
  - --color-disabled-text
  - --space-1
  - --space-2
  - --space-3
  - --border-radius-pill
  - --border-width-default
  - --border-width-emphasis
  - --font-family-primary
  - --font-size-body-base
  - --lineheight-body-base
  - --font-weight-regular
---

# Radio

Single-select choice from a group of mutually-exclusive options.
Mirrors the canonical
[Web-ODS Shared Library → Radio master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=666-36462&m=dev)
(`666:36462`). Visual architecture and state-circle mechanics mirror
the sibling Checkbox component; the only differences are the circular
geometry (no corners) and the centred dot glyph in the checked state.
Core default theme unit; brand packages supply theme token values.

## Summary

A radio is a 24 × 24 px circle that progressively enhances a native
`<input type="radio">`. Two persistent states paint the circle:
`unchecked` (hollow outline) and `checked` (solid fill with a centred
white dot). Three tone variants (`default` / `accent` / `critical`)
repaint the checked fill. The 32 × 32 px state-circle wash on hover /
focus / pressed mirrors the Checkbox's state mechanics verbatim — see
`src/components/checkbox/spec.md` § "Summary" for the token
substitution rationale. Linked Figma:
[`666:36462`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=666-36462&m=dev).

## Composes

Composes: none. The centred dot glyph is rendered as a CSS pseudo-element
on the box, not as a composed icon partial.

## Variant axes

| Axis | Values |
|---|---|
| `tone` | `default` / `accent` / `critical`. Affects the checked-state fill paint. Unchecked stays neutral in every tone. |
| `state` (transient) | `default` / `hover` / `focused` / `pressed`. Pseudo-class driven on the native input. State-circle wash paints behind the box for all three transient paints. |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Tone | Enum (`default` / `accent` / `critical`) | `tone` | yes | `"default"` | Drives the checked fill. |
| 2 | Checked | Boolean | `checked` | no | `false` | Persistent. Sets native `checked` + `aria-checked="true"`. |
| 3 | Disabled | Boolean | `disabled` | no | `false` | Persistent. Paints the `--color-disabled-*` trio, sets `aria-disabled="true"`, sets native `disabled`. |
| 4 | Name | Text | `name` | yes | `""` | Required for grouping — all radios with the same `name` participate in the same mutually-exclusive set. |
| 5 | Value | Text | `value` | yes | `""` | The value posted when this radio is selected within its `name` group. |
| 6 | Label | Text | `label` | no | `""` | Visible label rendered after the box. Wrapped inside the root `<label>`. |
| 7 | Accessible label | Text | `accessibleLabel` | when no `label` is set | `""` | Bound to the native input's `aria-label`. Required when no visible label is present. |

## Tokens consumed

**Structural** — `--border-radius-pill` (circular box + state-circle corners), `--space-1` (focus offset), `--space-3` (label gap), `--border-width-default` (focus ring weight), `--border-width-emphasis` (box outline weight).

**Surface** — `--color-bg-default` (unchecked box fill), `--color-border-strong` (unchecked outline), `--color-signal-info` (checked fill, tone `default`), `--color-signal-success` (checked fill, tone `accent`), `--color-signal-critical` (checked fill, tone `critical`).

**State circle** — `--color-text-secondary` mixed at 20 % / 30 % opacity via `color-mix()`, same as the Checkbox state-wash substitution.

**Content** — `--color-text-primary` (visible label), `--color-text-inverse` (centred dot), `--color-border-focus` (focus ring).

**Disabled** — `--color-disabled-bg` (box fill), `--color-disabled-border` (box outline), `--color-disabled-text` (dot + label).

**Typography** — `--font-family-primary`, `--font-size-body-base`, `--lineheight-body-base`, `--font-weight-regular` (label).

## Responsive behaviour

24 × 24 px footprint across every breakpoint (32 × 32 px state-circle
hit area). The optional label flows inline after the box and wraps if
the container narrows; `flex-shrink: 0` on the box keeps it at canonical
size.

## States

- `unchecked` — hollow 2 px outline using `--color-border-strong`. Box fill is `--color-bg-default`. No dot.
- `checked` — solid fill using the tone color, white 8 × 8 px dot centred.
- `hover` / `focused` / `pressed` — paint the 32 × 32 px state-circle wash behind the box (same mechanics as Checkbox).
- `disabled` — paints the `--color-disabled-*` trio.

The `[data-state]` hook on the root paints `hover` / `focused` /
`pressed` freezes for visual regression.

## Accessibility

- Uses native `<input type="radio">`; screen readers announce "Radio button, checked" / "Radio button, not checked" directly.
- `name` is required so the browser enforces single-selection across the group automatically.
- Keyboard: `Arrow` keys move selection within the group; `Tab` / `Shift+Tab` jump in/out of the group.
- Focus ring via `:focus-visible` only on keyboard navigation.
- WCAG: every (tone × state) combination meets AA non-text contrast.

## Design intent

Use a radio when the user must pick exactly one from a set of 2-7
mutually-exclusive options. For 8+ options, switch to a Select. For
multi-select, use a Checkbox.

## JavaScript API

### Init signature

| option | type | default | description |
|---|---|---|---|
| `commitOn` | `"input" \| "change"` | `"change"` | Which native event fires `other brand:radio:commit`. |

### Instance shape

```ts
type RadioInstance = {
  getChecked(): boolean;
  setChecked(next: boolean): void;
  setDisabled(disabled: boolean): void;
  destroy(): void;
};
```

### Events

| name | detail | when fired |
|---|---|---|
| `other brand:radio:change` | `{ checked: boolean, name: string, value: string }` | Native `change` on the input. |
| `other brand:radio:commit` | `{ checked: boolean, name: string, value: string, source: "pointer" \| "keyboard" \| "api" }` | After commit per the `commitOn` option. |

### State attributes

| attribute | values | when applied |
|---|---|---|
| `data-component` | `"radio"` | Always on the root `<label>`. |
| `data-tone` | `"default" \| "accent" \| "critical"` | Always on the root. |
| `data-state` | `"hover" \| "focused" \| "pressed"` | Story-only freeze hook. |
| `aria-disabled` | `"true"` | On the root when `disabled=true`. |

### Keyboard

| key | behavior |
|---|---|
| `Arrow Up` / `Arrow Down` / `Arrow Left` / `Arrow Right` | Move selection within the radio group. |
| `Tab` / `Shift+Tab` | Move focus in / out of the group. |

### SSR fallback

Native `<input type="radio">` ships in the template, so single-select
group enforcement, keyboard navigation, and screen-reader semantics
all work without JS. The JS layer's only jobs are mirroring the
input's `checked` state to the root's `is-checked` modifier and
dispatching the namespaced `other brand:radio:*` CustomEvents.


### radio.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "radio",
  "status": "published",
  "summary": "Single-select choice from a group of mutually-exclusive options. Mirrors the canonical [Web-ODS Shared Library → Radio master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=666-36462&m=dev) (`666:36462`). Visual architecture and state-circle mechanics mirror the sibling Checkbox component; the only differences are the circular geometry (no corners) and the centred dot glyph in the checked state. Core default theme unit; brand packages supply theme token values.",
  "specUrl": "/api/components/radio/spec.md",
  "sourceFiles": {
    "spec": "/api/components/radio/spec.md",
    "hbs": "/api/components/radio/source/radio.hbs",
    "scss": "/api/components/radio/source/radio.scss",
    "js": "/api/components/radio/source/radio.js"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "666:36462",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=666-36462&m=dev"
  },
  "rootClass": "c-radio",
  "bemClasses": [
    "c-radio"
  ],
  "properties": [
    {
      "name": "tone",
      "fixtureKey": "tone",
      "type": "Enum (`default` / `accent` / `critical`)",
      "required": true,
      "default": "\"default\"",
      "description": "Drives the checked fill.",
      "figmaLabel": "Tone"
    },
    {
      "name": "checked",
      "fixtureKey": "checked",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent. Sets native `checked` + `aria-checked=\"true\"`.",
      "figmaLabel": "Checked"
    },
    {
      "name": "disabled",
      "fixtureKey": "disabled",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent. Paints the `--color-disabled-*` trio, sets `aria-disabled=\"true\"`, sets native `disabled`.",
      "figmaLabel": "Disabled"
    },
    {
      "name": "name",
      "fixtureKey": "name",
      "type": "Text",
      "required": true,
      "default": "\"\"",
      "description": "Required for grouping — all radios with the same `name` participate in the same mutually-exclusive set.",
      "figmaLabel": "Name"
    },
    {
      "name": "value",
      "fixtureKey": "value",
      "type": "Text",
      "required": true,
      "default": "\"\"",
      "description": "The value posted when this radio is selected within its `name` group.",
      "figmaLabel": "Value"
    },
    {
      "name": "label",
      "fixtureKey": "label",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Visible label rendered after the box. Wrapped inside the root `<label>`.",
      "figmaLabel": "Label"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Bound to the native input's `aria-label`. Required when no visible label is present.",
      "figmaLabel": "Accessible label"
    }
  ],
  "variants": [
    {
      "axis": "tone",
      "values": "`default` / `accent` / `critical`. Affects the checked-state fill paint. Unchecked stays neutral in every tone."
    },
    {
      "axis": "`state` (transient)",
      "values": "`default` / `hover` / `focused` / `pressed`. Pseudo-class driven on the native input. State-circle wash paints behind the box for all three transient paints."
    }
  ],
  "tokensConsumed": [
    "--color-bg-default",
    "--color-border-strong",
    "--color-border-focus",
    "--color-signal-info",
    "--color-signal-success",
    "--color-signal-critical",
    "--color-text-primary",
    "--color-text-secondary",
    "--color-text-inverse",
    "--color-disabled-bg",
    "--color-disabled-border",
    "--color-disabled-text",
    "--space-1",
    "--space-2",
    "--space-3",
    "--border-radius-pill",
    "--border-width-default",
    "--border-width-emphasis",
    "--font-family-primary",
    "--font-size-body-base",
    "--lineheight-body-base",
    "--font-weight-regular"
  ],
  "composes": [],
  "hasBehavior": true,
  "accessibility": [
    "Uses native `<input type=\"radio\">`; screen readers announce \"Radio button, checked\" / \"Radio button, not checked\" directly.",
    "`name` is required so the browser enforces single-selection across the group automatically.",
    "Keyboard: `Arrow` keys move selection within the group; `Tab` / `Shift+Tab` jump in/out of the group.",
    "Focus ring via `:focus-visible` only on keyboard navigation.",
    "WCAG: every (tone × state) combination meets AA non-text contrast."
  ],
  "storybookId": "molecules-radio",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## component/switch

---
type: component
name: switch
subtype: molecule
librarySource: brand
figmaFileKey: 0o8SL5BEk8wHtgud00dRyg
figmaNodeId: "717:48394"
status: published
behavior: true
composes:
  - icon
tokensConsumed:
  - --color-bg-default
  - --color-border-subtle
  - --color-border-focus
  - --color-signal-info
  - --color-signal-success
  - --color-signal-critical
  - --color-text-primary
  - --color-text-secondary
  - --color-text-inverse
  - --color-disabled-bg
  - --color-disabled-border
  - --color-disabled-text
  - --space-1
  - --space-2
  - --space-3
  - --border-radius-pill
  - --border-width-default
  - --font-family-primary
  - --font-size-body-base
  - --lineheight-body-base
  - --font-weight-regular
---

# Switch

Binary on/off toggle for committing a setting immediately when the
control changes. Mirrors the canonical
[Web-ODS Shared Library → Switch master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=717-48394&m=dev)
(`717:48394`) and its text-label compositions (`732:49743`,
`732:49642`, `732:49755`, `732:49666`). Every visual binds to
other brand `THEME = other brand` + `PLATFORM = other brand / Desktop|Tablet|Mobile`
Variable resolutions — the multi-brand master is consumed under the
other brand mode pick per the contract in
[`storybook-other brand-figma-modes.mdc`](../../../../.cursor/rules/storybook-other brand-figma-modes.mdc).

## Summary

The Switch is a stateful checkbox dressed as a 42 × 26 px pill. It
progressively enhances a native `<input type="checkbox">` with
`role="switch"` + `aria-checked` semantics so screen readers
announce on/off state directly. The knob inside the track measures
18 × 18 px with a 4 px inset on every side (slide travel = 42 - 18 -
8 = 16 px). Three persistent tone variants
(`default` / `success` / `critical`) repaint the ON-state track
while leaving the knob white in every variant. Hover / pressed
paint a 32 × 32 px **state circle** behind the knob — Figma source
is the default theme `inverse-secondary` token at 20 % (hover) /
10 % (pressed) opacity; other brand has no `inverse-secondary` alias
today, so the implementation reuses the same `--color-text-secondary`
substitution Checkbox / Radio adopt. The halo travels with the knob
under `.is-checked` so it always sits centered behind it. Disabled
and loading are persistent and opt-in via boolean props.
Linked Figma:
[`717:48394`](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=717-48394&m=dev).

## Composes

Composes: `icon` — the optional `status/simple-checkmark-small`
glyph that paints inside the knob when
`showIcon=true && checked=true`. The glyph wrapper is **always
rendered** when `showIcon !== false` (and `loading !== true`) and its
visibility is CSS-driven via `opacity` keyed on the `is-checked`
modifier class, so a JS click that flips `is-checked` doesn't have to
insert nodes.

### Geometry inventory (icon slots)

Figma knob Icon mask wrapper is **18×18** (`717:48382`). Icon enum has
no `18` — nearest is `size="16"`. Do **not** stretch `.c-icon` to the
18 px knob via consumer CSS (control-pad / box override forbidden).

| slot | outer px | pad | icon INSTANCE px | size= | frame= | wrapper pad? |
|---|---|---|---|---|---|---|
| `__glyph` (checkmark) | 18×18 knob | none on icon | 18×18 Figma / 16 code | `16` | none | no |

## Variant axes

| Axis | Values |
|---|---|
| `tone` | `default` (track paints `--color-signal-info` on ON) / `success` (track paints `--color-signal-success`) / `critical` (track paints `--color-signal-critical`). Tone only affects the ON paint; the OFF track is the neutral border-subtle fill in every tone. |
| `state` (transient) | `default` / `hover` / `focused` / `pressed`. Pseudo-class driven (`:hover` / `:focus-visible` / `:active` on the native input). The `[data-state]` hook on the root lets the AllStyles gallery freeze each paint for visual regression. |

## Properties

| # | Figma label | Content type | Code identifier | Required | Default | Notes |
|---|---|---|---|---|---|---|
| 1 | Tone | Enum (`default` / `success` / `critical`) | `tone` | yes | `"default"` | Drives the ON-state track paint. Mirrors the Figma `Tone` property. |
| 2 | Checked | Boolean | `checked` | no | `false` | Persistent state. Sets the native `<input>` `checked` attribute and `aria-checked="true"` on the input; the knob slides right via the `is-checked` class on the root. |
| 3 | Disabled | Boolean | `disabled` | no | `false` | Persistent state. Paints the `--color-disabled-*` token trio, adds `aria-disabled="true"` on the root, and sets the native `disabled` attribute on the input (removes it from the tab order). |
| 4 | Loading | Boolean | `loading` | no | `false` | Persistent state. Replaces the knob's icon slot with a spinner and locks the input via `aria-busy="true"` on the root. Click commits are ignored while loading. |
| 5 | Show icon | Boolean | `showIcon` | no | `true` | Renders the checkmark glyph inside the knob when checked. When `false`, the knob paints as a solid circle. |
| 6 | Label | Text | `label` | no | `""` | Optional visible label rendered after the track (BEM element `c-switch__label`). When omitted, the switch is icon-only — consumers MUST supply an `aria-label` via the `accessibleLabel` prop. |
| 7 | Accessible label | Text | `accessibleLabel` | when no `label` is set | `""` | Bound to the native input's `aria-label`. Required when no visible label is present so the switch's purpose is announced. |
| 8 | Name | Text | `name` | no | `""` | Bound to the native `<input>` `name` attribute for form submission. |
| 9 | Value | Text | `value` | no | `"on"` | Bound to the native `<input>` `value` attribute (the value posted when checked). |

## Tokens consumed

**Structural** — `--space-1` (2px focus offset), `--space-2` (knob-track gap), `--space-3` (label gap), `--border-radius-pill` (track + knob), `--border-width-default` (focus ring weight).

**Surface** — `--color-bg-default` (knob fill), `--color-border-subtle` (OFF track fill), `--color-signal-info` (ON track, tone `default`), `--color-signal-success` (ON track, tone `success`), `--color-signal-critical` (ON track, tone `critical`).

**State circle** — `--color-text-secondary` mixed at 20 % / 10 % opacity via `color-mix()` for the 32 × 32 px hover / pressed halo painted behind the knob. Substitutes for the default theme `inverse-secondary` source until other brand publishes a dedicated state-wash alias (same substitution Checkbox + Radio apply).

**Content** — `--color-text-primary` (visible label), `--color-text-inverse` (knob icon on ON track), `--color-border-focus` (focus ring).

**Disabled** — `--color-disabled-bg` (track), `--color-disabled-border` (track outline), `--color-disabled-text` (knob).

**Typography** — `--font-family-primary`, `--font-size-body-base`, `--lineheight-body-base`, `--font-weight-regular` (label).

See `storybook-other brand/.storybook/preview.scss` for the surfaced
custom-property values.

## Responsive behaviour

The switch paints at a fixed 42 × 26 px footprint across every
other brand breakpoint (SM / MD / LG / XL). The optional label flows
inline after the track and wraps if the container narrows;
`flex-shrink: 0` on the track keeps the pill at its canonical size.
The whole control is RTL-ready — all inline-axis sizing uses
logical properties (`padding-inline`, `inset-inline-start`) so the
knob slides toward the inline-end edge under `dir="rtl"`.

## States

- `default` — OFF: gray track + knob nestled inline-start. ON: tone-coloured track + knob nestled inline-end. Paint via the `is-checked` modifier.
- `hover` — track paint shifts slightly via CSS `:hover` on the native input AND a 32 × 32 px state circle paints behind the knob at 20 % opacity (the default theme `inverse-secondary` source, substituted with `--color-text-secondary` under other brand).
- `focused` — `:focus-visible` on the native input draws a 2 px ring on the track using `--color-border-focus` at `--space-1` offset.
- `pressed` — `:active` on the native input lowers the same state-circle halo to 10 % opacity behind the knob (matches the Figma `717:48394` pressed paint). The knob itself does not transform — the halo is the entire press feedback.
- `disabled` — paints the `--color-disabled-*` trio, sets `aria-disabled="true"` on the root, sets `disabled` on the input.
- `loading` — replaces the knob's icon slot with a spinner; the input remains in the tab order but click commits are ignored. Sets `aria-busy="true"` on the root.

The AllStyles gallery's `[data-state]` hook on the root paints
`hover` / `focused` / `pressed` freezes for visual regression
without requiring user input.

## Accessibility

- Uses native `<input type="checkbox">` with `role="switch"` + `aria-checked` for the binary state — screen readers announce "On" / "Off" directly.
- Always carries either a visible `<label>` (text after the track) or an `aria-label` via `accessibleLabel`. The label is wrapped in the root `<label>` element so clicking the text toggles the switch.
- Focus ring uses `:focus-visible` so it only appears on keyboard navigation; pointer focus stays quiet.
- Keyboard: `Space` toggles. `Tab` / `Shift+Tab` move focus.
- WCAG: track surfaces meet AA non-text contrast against `--color-bg-default` in every tone × state combination. Disabled paint meets WCAG 2.2 § 1.4.11 by retaining a visible outline.
- Reduced motion: the knob's slide transition is removed when `prefers-reduced-motion: reduce` is set.

## Design intent

A switch commits a change immediately when toggled — use it for
settings that take effect right away (notifications on/off, sync
toggle). For form inputs that submit later, use a Checkbox
instead.

## JavaScript API

### Init signature

| option | type | default | description |
|---|---|---|---|
| `commitOn` | `"input" \| "change"` | `"change"` | Which native event fires the `other brand:switch:commit` CustomEvent. |

### Instance shape

```ts
type SwitchInstance = {
  getChecked(): boolean;
  setChecked(next: boolean): void;
  setDisabled(disabled: boolean): void;
  destroy(): void;
};
```

### Events

| name | detail | when fired |
|---|---|---|
| `other brand:switch:change` | `{ checked: boolean }` | Native `change` on the underlying input. |
| `other brand:switch:commit` | `{ checked: boolean, source: "pointer" \| "keyboard" \| "api" }` | After commit per the `commitOn` option. |

### State attributes

| attribute | values | when applied |
|---|---|---|
| `data-component` | `"switch"` | Always on the root `<label>`. |
| `data-tone` | `"default" \| "success" \| "critical"` | Always on the root. |
| `data-state` | `"hover" \| "focused" \| "pressed"` | Story-only freeze hook for the AllStyles gallery. |
| `aria-disabled` | `"true"` | On the root when `disabled=true`. |
| `aria-busy` | `"true"` | On the root when `loading=true`. |

### Keyboard

| key | behavior |
|---|---|
| `Space` | Toggle the switch. |
| `Tab` / `Shift+Tab` | Move focus to / from the input. |

### SSR fallback

The native `<input type="checkbox" role="switch">` ships in the
template, so the switch toggles fully without JS — pointer + keyboard
both work. The JS layer's only jobs are to dispatch the namespaced
`other brand:switch:*` CustomEvents and to enforce the no-op while
`loading=true`.


### switch.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/component.v2.json",
  "type": "component",
  "subtype": "molecule",
  "librarySource": "brand",
  "name": "switch",
  "status": "published",
  "summary": "Binary on/off toggle for committing a setting immediately when the control changes. Mirrors the canonical [Web-ODS Shared Library → Switch master](https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/Web-ODS-Shared-Library?node-id=717-48394&m=dev) (`717:48394`) and its text-label compositions (`732:49743`, `732:49642`, `732:49755`, `732:49666`). Every visual binds to other brand `THEME = other brand` + `PLATFORM = other brand / Desktop|Tablet|Mobile` Variable resolutions — the multi-brand master is consumed under the other brand mode pick per the contract in [`storybook-other brand-figma-modes.",
  "specUrl": "/api/components/switch/spec.md",
  "sourceFiles": {
    "spec": "/api/components/switch/spec.md",
    "hbs": "/api/components/switch/source/switch.hbs",
    "scss": "/api/components/switch/source/switch.scss",
    "js": "/api/components/switch/source/switch.js"
  },
  "figma": {
    "fileKey": "0o8SL5BEk8wHtgud00dRyg",
    "nodeId": "717:48394",
    "url": "https://www.figma.com/design/0o8SL5BEk8wHtgud00dRyg/?node-id=717-48394&m=dev"
  },
  "rootClass": "c-icon",
  "bemClasses": [
    "c-icon",
    "c-switch"
  ],
  "properties": [
    {
      "name": "tone",
      "fixtureKey": "tone",
      "type": "Enum (`default` / `success` / `critical`)",
      "required": true,
      "default": "\"default\"",
      "description": "Drives the ON-state track paint. Mirrors the Figma `Tone` property.",
      "figmaLabel": "Tone"
    },
    {
      "name": "checked",
      "fixtureKey": "checked",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent state. Sets the native `<input>` `checked` attribute and `aria-checked=\"true\"` on the input; the knob slides right via the `is-checked` class on the root.",
      "figmaLabel": "Checked"
    },
    {
      "name": "disabled",
      "fixtureKey": "disabled",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent state. Paints the `--color-disabled-*` token trio, adds `aria-disabled=\"true\"` on the root, and sets the native `disabled` attribute on the input (removes it from the tab order).",
      "figmaLabel": "Disabled"
    },
    {
      "name": "loading",
      "fixtureKey": "loading",
      "type": "Boolean",
      "required": false,
      "default": "false",
      "description": "Persistent state. Replaces the knob's icon slot with a spinner and locks the input via `aria-busy=\"true\"` on the root. Click commits are ignored while loading.",
      "figmaLabel": "Loading"
    },
    {
      "name": "showIcon",
      "fixtureKey": "showIcon",
      "type": "Boolean",
      "required": false,
      "default": "true",
      "description": "Renders the checkmark glyph inside the knob when checked. When `false`, the knob paints as a solid circle.",
      "figmaLabel": "Show icon"
    },
    {
      "name": "label",
      "fixtureKey": "label",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Optional visible label rendered after the track (BEM element `c-switch__label`). When omitted, the switch is icon-only — consumers MUST supply an `aria-label` via the `accessibleLabel` prop.",
      "figmaLabel": "Label"
    },
    {
      "name": "accessibleLabel",
      "fixtureKey": "accessibleLabel",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Bound to the native input's `aria-label`. Required when no visible label is present so the switch's purpose is announced.",
      "figmaLabel": "Accessible label"
    },
    {
      "name": "name",
      "fixtureKey": "name",
      "type": "Text",
      "required": false,
      "default": "\"\"",
      "description": "Bound to the native `<input>` `name` attribute for form submission.",
      "figmaLabel": "Name"
    },
    {
      "name": "value",
      "fixtureKey": "value",
      "type": "Text",
      "required": false,
      "default": "\"on\"",
      "description": "Bound to the native `<input>` `value` attribute (the value posted when checked).",
      "figmaLabel": "Value"
    }
  ],
  "variants": [
    {
      "axis": "tone",
      "values": "`default` (track paints `--color-signal-info` on ON) / `success` (track paints `--color-signal-success`) / `critical` (track paints `--color-signal-critical`). Tone only affects the ON paint; the OFF track is the neutral border-subtle fill in every tone."
    },
    {
      "axis": "`state` (transient)",
      "values": "`default` / `hover` / `focused` / `pressed`. Pseudo-class driven (`:hover` / `:focus-visible` / `:active` on the native input). The `[data-state]` hook on the root lets the AllStyles gallery freeze each paint for visual regression."
    }
  ],
  "tokensConsumed": [
    "--color-bg-default",
    "--color-border-subtle",
    "--color-border-focus",
    "--color-signal-info",
    "--color-signal-success",
    "--color-signal-critical",
    "--color-text-primary",
    "--color-text-secondary",
    "--color-text-inverse",
    "--color-disabled-bg",
    "--color-disabled-border",
    "--color-disabled-text",
    "--space-1",
    "--space-2",
    "--space-3",
    "--border-radius-pill",
    "--border-width-default",
    "--font-family-primary",
    "--font-size-body-base",
    "--lineheight-body-base",
    "--font-weight-regular"
  ],
  "composes": [
    "icon"
  ],
  "hasBehavior": true,
  "accessibility": [
    "Uses native `<input type=\"checkbox\">` with `role=\"switch\"` + `aria-checked` for the binary state — screen readers announce \"On\" / \"Off\" directly.",
    "Always carries either a visible `<label>` (text after the track) or an `aria-label` via `accessibleLabel`. The label is wrapped in the root `<label>` element so clicking the text toggles the switch.",
    "Focus ring uses `:focus-visible` so it only appears on keyboard navigation; pointer focus stays quiet.",
    "Keyboard: `Space` toggles. `Tab` / `Shift+Tab` move focus.",
    "WCAG: track surfaces meet AA non-text contrast against `--color-bg-default` in every tone × state combination. Disabled paint meets WCAG 2.2 § 1.4.11 by retaining a visible outline.",
    "Reduced motion: the knob's slide transition is removed when `prefers-reduced-motion: reduce` is set."
  ],
  "storybookId": "molecules-switch",
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/borders

---
type: token
name: borders
status: published
---

# Borders

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--border-radius-0` | `0` | |
| `--border-radius-xs` | `2px` | |
| `--border-radius-s` | `4px` | |
| `--border-radius-m` | `6px` | |
| `--border-radius-l` | `8px` | |
| `--border-radius-xl` | `16px` | |
| `--border-radius-xxl` | `24px` | |
| `--border-radius-xxxl` | `30px` | |
| `--border-width-0` | `0` | |
| `--border-width-xs` | `1px` | |
| `--border-width-s` | `2px` | |
| `--border-width-m` | `4px` | |
| `--border-width-l` | `6px` | |
| `--border-width-xl` | `8px` | |
| `--border-radius-control` | `var(--border-radius-s)` | |
| `--border-radius-card` | `var(--border-radius-l)` | |
| `--border-radius-dialog` | `var(--border-radius-xl)` | |
| `--border-radius-pill` | `9999px` | |
| `--border-width-none` | `var(--border-width-0)` | |
| `--border-width-hairline` | `var(--border-width-xs)` | |
| `--border-width-default` | `var(--border-width-s)` | |
| `--border-width-emphasis` | `var(--border-width-m)` | |
| `--border-width-focus` | `var(--border-width-m)` | |


### borders.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "borders",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/borders/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/borders/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "borders",
      "cssVar": "--border-radius-0",
      "path": [
        "radius",
        "0"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-xs",
      "path": [
        "radius",
        "xs"
      ],
      "$type": "dimension",
      "$value": "2px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-s",
      "path": [
        "radius",
        "s"
      ],
      "$type": "dimension",
      "$value": "4px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-m",
      "path": [
        "radius",
        "m"
      ],
      "$type": "dimension",
      "$value": "6px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-l",
      "path": [
        "radius",
        "l"
      ],
      "$type": "dimension",
      "$value": "8px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-xl",
      "path": [
        "radius",
        "xl"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-xxl",
      "path": [
        "radius",
        "xxl"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-xxxl",
      "path": [
        "radius",
        "xxxl"
      ],
      "$type": "dimension",
      "$value": "30px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-0",
      "path": [
        "width",
        "0"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-xs",
      "path": [
        "width",
        "xs"
      ],
      "$type": "dimension",
      "$value": "1px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-s",
      "path": [
        "width",
        "s"
      ],
      "$type": "dimension",
      "$value": "2px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-m",
      "path": [
        "width",
        "m"
      ],
      "$type": "dimension",
      "$value": "4px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-l",
      "path": [
        "width",
        "l"
      ],
      "$type": "dimension",
      "$value": "6px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-xl",
      "path": [
        "width",
        "xl"
      ],
      "$type": "dimension",
      "$value": "8px"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-control",
      "path": [
        "radius",
        "control"
      ],
      "$type": "dimension",
      "$value": "var(--border-radius-s)"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-card",
      "path": [
        "radius",
        "card"
      ],
      "$type": "dimension",
      "$value": "var(--border-radius-l)"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-dialog",
      "path": [
        "radius",
        "dialog"
      ],
      "$type": "dimension",
      "$value": "var(--border-radius-xl)"
    },
    {
      "family": "borders",
      "cssVar": "--border-radius-pill",
      "path": [
        "radius",
        "pill"
      ],
      "$type": "dimension",
      "$value": "9999px"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-none",
      "path": [
        "width",
        "none"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-0)"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-hairline",
      "path": [
        "width",
        "hairline"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-xs)"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-default",
      "path": [
        "width",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-s)"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-emphasis",
      "path": [
        "width",
        "emphasis"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-m)"
    },
    {
      "family": "borders",
      "cssVar": "--border-width-focus",
      "path": [
        "width",
        "focus"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-m)"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/button

---
type: token
name: button
status: published
---

# Button

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--button-primary-bg-default` | `var(--color-bg-brand)` | |
| `--button-primary-bg-hover` | `var(--color-text-accent)` | |
| `--button-primary-bg-pressed` | `var(--color-bg-inverse-strong)` | |
| `--button-primary-content-default` | `var(--color-text-inverse)` | |
| `--button-primary-content-hover` | `var(--color-text-inverse)` | |
| `--button-primary-content-pressed` | `var(--color-text-inverse)` | |
| `--button-primary-border` | `var(--color-text-accent)` | |
| `--button-secondary-bg-default` | `var(--color-bg-subtle)` | |
| `--button-secondary-bg-hover` | `var(--color-canvas-contrast)` | |
| `--button-secondary-bg-pressed` | `var(--color-bg-muted)` | |
| `--button-secondary-content-default` | `var(--color-text-primary)` | |
| `--button-secondary-content-hover` | `var(--color-text-primary)` | |
| `--button-secondary-content-pressed` | `var(--color-text-primary)` | |
| `--button-secondary-border` | `transparent` | |
| `--button-tertiary-bg-default` | `var(--color-bg-muted)` | |
| `--button-tertiary-bg-hover` | `var(--color-bg-subtle)` | |
| `--button-tertiary-bg-pressed` | `var(--color-canvas-contrast)` | |
| `--button-tertiary-content-default` | `var(--color-text-primary)` | |
| `--button-tertiary-content-hover` | `var(--color-text-primary)` | |
| `--button-tertiary-content-pressed` | `var(--color-text-primary)` | |
| `--button-tertiary-border` | `var(--color-border-subtle)` | |
| `--button-inverse-bg-default` | `var(--color-bg-inverse-strong)` | |
| `--button-inverse-bg-hover` | `var(--color-bg-brand)` | |
| `--button-inverse-bg-pressed` | `var(--color-text-accent)` | |
| `--button-inverse-content-default` | `var(--color-text-inverse)` | |
| `--button-inverse-content-hover` | `var(--color-text-inverse)` | |
| `--button-inverse-content-pressed` | `var(--color-text-inverse)` | |
| `--button-inverse-border` | `var(--color-text-accent)` | |
| `--button-text-bg-default` | `transparent` | |
| `--button-text-bg-hover` | `var(--color-bg-subtle)` | |
| `--button-text-bg-pressed` | `var(--color-canvas-contrast)` | |
| `--button-text-content-default` | `var(--color-text-accent)` | |
| `--button-text-content-hover` | `var(--color-text-accent)` | |
| `--button-text-content-pressed` | `var(--color-text-accent)` | |
| `--button-primary-ghost-bg-default` | `transparent` | |
| `--button-primary-ghost-bg-hover` | `var(--color-bg-subtle)` | |
| `--button-primary-ghost-bg-pressed` | `var(--color-canvas-contrast)` | |
| `--button-primary-ghost-content-default` | `var(--color-bg-brand)` | |
| `--button-primary-ghost-content-hover` | `var(--color-bg-brand)` | |
| `--button-primary-ghost-content-pressed` | `var(--color-bg-brand)` | |
| `--button-primary-ghost-border` | `var(--color-bg-brand)` | |
| `--button-secondary-ghost-bg-default` | `transparent` | |
| `--button-secondary-ghost-bg-hover` | `color-mix(
    in srgb,
    var(--color-text-inverse) 20%,
    transparent
  )` | |
| `--button-secondary-ghost-bg-pressed` | `color-mix(
    in srgb,
    var(--color-text-inverse) 10%,
    transparent
  )` | |
| `--button-secondary-ghost-content-default` | `var(--color-text-inverse)` | |
| `--button-secondary-ghost-content-hover` | `var(--color-text-inverse)` | |
| `--button-secondary-ghost-content-pressed` | `var(--color-text-inverse)` | |
| `--button-secondary-ghost-border` | `var(--color-text-inverse)` | |
| `--button-gradient-bg-default` | `linear-gradient(
    172deg,
    var(--color-text-accent) 19%,
    var(--color-bg-brand) 120%
  )` | |
| `--button-gradient-bg-hover` | `linear-gradient(
    167deg,
    var(--color-text-accent) 26%,
    var(--color-bg-inverse-strong) 105%
  )` | |
| `--button-gradient-bg-pressed` | `linear-gradient(
    164deg,
    var(--color-bg-brand) 28%,
    var(--color-bg-inverse-strong) 101%
  )` | |
| `--button-gradient-content-default` | `var(--color-text-inverse)` | |
| `--button-gradient-content-hover` | `var(--color-text-inverse)` | |
| `--button-gradient-content-pressed` | `var(--color-text-inverse)` | |
| `--button-gradient-border` | `var(--color-text-accent)` | |
| `--button-disabled-bg` | `var(--color-disabled-bg)` | |
| `--button-disabled-content` | `var(--color-disabled-text)` | |
| `--button-disabled-border` | `var(--color-disabled-border)` | |
| `--button-focus-ring` | `var(--color-border-focus)` | |
| `--button-spinner-track` | `var(--color-border-subtle)` | |
| `--button-spinner-fill` | `var(--color-text-accent)` | |
| `--button-radius` | `var(--border-radius-pill)` | |
| `--button-radius-focus` | `var(--border-radius-pill)` | |
| `--button-border-width-primary` | `var(--border-width-none)` | |
| `--button-border-width-secondary` | `var(--border-width-none)` | |
| `--button-border-width-ghost` | `var(--border-width-hairline)` | |
| `--button-border-width-focus` | `var(--border-width-default)` | |
| `--button-block-size-s` | `24px` | |
| `--button-block-size-m` | `36px` | |
| `--button-block-size-l` | `40px` | |
| `--button-block-size-xl` | `48px` | |
| `--button-min-inline-size` | `48px` | |
| `--button-padding-block-s` | `var(--space-3)` | |
| `--button-padding-block-m` | `var(--space-3)` | |
| `--button-padding-block-l` | `var(--space-3)` | |
| `--button-padding-block-xl` | `var(--space-4)` | |
| `--button-padding-inline-s` | `var(--space-3)` | |
| `--button-padding-inline-m` | `var(--space-4)` | |
| `--button-padding-inline-l` | `var(--space-5)` | |
| `--button-padding-inline-xl` | `var(--space-7)` | |
| `--button-gap` | `var(--space-3)` | |
| `--button-icon-size-s` | `16px` | |
| `--button-icon-size-m` | `20px` | |
| `--button-icon-size-l` | `20px` | |
| `--button-icon-size-xl` | `20px` | |


### button.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "button",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/button/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/button/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "button",
      "cssVar": "--button-primary-bg-default",
      "path": [
        "primary",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-bg-hover",
      "path": [
        "primary",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-bg-pressed",
      "path": [
        "primary",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-inverse-strong)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-content-default",
      "path": [
        "primary",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-content-hover",
      "path": [
        "primary",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-content-pressed",
      "path": [
        "primary",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-border",
      "path": [
        "primary",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-bg-default",
      "path": [
        "secondary",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-bg-hover",
      "path": [
        "secondary",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-canvas-contrast)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-bg-pressed",
      "path": [
        "secondary",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-muted)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-content-default",
      "path": [
        "secondary",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-content-hover",
      "path": [
        "secondary",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-content-pressed",
      "path": [
        "secondary",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-border",
      "path": [
        "secondary",
        "border"
      ],
      "$type": "dimension",
      "$value": "transparent"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-bg-default",
      "path": [
        "tertiary",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-muted)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-bg-hover",
      "path": [
        "tertiary",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-bg-pressed",
      "path": [
        "tertiary",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-canvas-contrast)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-content-default",
      "path": [
        "tertiary",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-content-hover",
      "path": [
        "tertiary",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-content-pressed",
      "path": [
        "tertiary",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-primary)"
    },
    {
      "family": "button",
      "cssVar": "--button-tertiary-border",
      "path": [
        "tertiary",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-border-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-bg-default",
      "path": [
        "inverse",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-inverse-strong)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-bg-hover",
      "path": [
        "inverse",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-bg-pressed",
      "path": [
        "inverse",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-content-default",
      "path": [
        "inverse",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-content-hover",
      "path": [
        "inverse",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-content-pressed",
      "path": [
        "inverse",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-inverse-border",
      "path": [
        "inverse",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-text-bg-default",
      "path": [
        "text",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "transparent"
    },
    {
      "family": "button",
      "cssVar": "--button-text-bg-hover",
      "path": [
        "text",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-text-bg-pressed",
      "path": [
        "text",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-canvas-contrast)"
    },
    {
      "family": "button",
      "cssVar": "--button-text-content-default",
      "path": [
        "text",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-text-content-hover",
      "path": [
        "text",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-text-content-pressed",
      "path": [
        "text",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-bg-default",
      "path": [
        "primary",
        "ghost",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "transparent"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-bg-hover",
      "path": [
        "primary",
        "ghost",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-bg-pressed",
      "path": [
        "primary",
        "ghost",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-canvas-contrast)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-content-default",
      "path": [
        "primary",
        "ghost",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-content-hover",
      "path": [
        "primary",
        "ghost",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-content-pressed",
      "path": [
        "primary",
        "ghost",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-primary-ghost-border",
      "path": [
        "primary",
        "ghost",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-bg-brand)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-bg-default",
      "path": [
        "secondary",
        "ghost",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "transparent"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-bg-hover",
      "path": [
        "secondary",
        "ghost",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "color-mix(\n    in srgb,\n    var(--color-text-inverse) 20%,\n    transparent\n  )"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-bg-pressed",
      "path": [
        "secondary",
        "ghost",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "color-mix(\n    in srgb,\n    var(--color-text-inverse) 10%,\n    transparent\n  )"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-content-default",
      "path": [
        "secondary",
        "ghost",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-content-hover",
      "path": [
        "secondary",
        "ghost",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-content-pressed",
      "path": [
        "secondary",
        "ghost",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-secondary-ghost-border",
      "path": [
        "secondary",
        "ghost",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-bg-default",
      "path": [
        "gradient",
        "bg",
        "default"
      ],
      "$type": "dimension",
      "$value": "linear-gradient(\n    172deg,\n    var(--color-text-accent) 19%,\n    var(--color-bg-brand) 120%\n  )"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-bg-hover",
      "path": [
        "gradient",
        "bg",
        "hover"
      ],
      "$type": "dimension",
      "$value": "linear-gradient(\n    167deg,\n    var(--color-text-accent) 26%,\n    var(--color-bg-inverse-strong) 105%\n  )"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-bg-pressed",
      "path": [
        "gradient",
        "bg",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "linear-gradient(\n    164deg,\n    var(--color-bg-brand) 28%,\n    var(--color-bg-inverse-strong) 101%\n  )"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-content-default",
      "path": [
        "gradient",
        "content",
        "default"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-content-hover",
      "path": [
        "gradient",
        "content",
        "hover"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-content-pressed",
      "path": [
        "gradient",
        "content",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-inverse)"
    },
    {
      "family": "button",
      "cssVar": "--button-gradient-border",
      "path": [
        "gradient",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-disabled-bg",
      "path": [
        "disabled",
        "bg"
      ],
      "$type": "dimension",
      "$value": "var(--color-disabled-bg)"
    },
    {
      "family": "button",
      "cssVar": "--button-disabled-content",
      "path": [
        "disabled",
        "content"
      ],
      "$type": "dimension",
      "$value": "var(--color-disabled-text)"
    },
    {
      "family": "button",
      "cssVar": "--button-disabled-border",
      "path": [
        "disabled",
        "border"
      ],
      "$type": "dimension",
      "$value": "var(--color-disabled-border)"
    },
    {
      "family": "button",
      "cssVar": "--button-focus-ring",
      "path": [
        "focus",
        "ring"
      ],
      "$type": "dimension",
      "$value": "var(--color-border-focus)"
    },
    {
      "family": "button",
      "cssVar": "--button-spinner-track",
      "path": [
        "spinner",
        "track"
      ],
      "$type": "dimension",
      "$value": "var(--color-border-subtle)"
    },
    {
      "family": "button",
      "cssVar": "--button-spinner-fill",
      "path": [
        "spinner",
        "fill"
      ],
      "$type": "dimension",
      "$value": "var(--color-text-accent)"
    },
    {
      "family": "button",
      "cssVar": "--button-radius",
      "path": [
        "radius"
      ],
      "$type": "dimension",
      "$value": "var(--border-radius-pill)"
    },
    {
      "family": "button",
      "cssVar": "--button-radius-focus",
      "path": [
        "radius",
        "focus"
      ],
      "$type": "dimension",
      "$value": "var(--border-radius-pill)"
    },
    {
      "family": "button",
      "cssVar": "--button-border-width-primary",
      "path": [
        "border",
        "width",
        "primary"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-none)"
    },
    {
      "family": "button",
      "cssVar": "--button-border-width-secondary",
      "path": [
        "border",
        "width",
        "secondary"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-none)"
    },
    {
      "family": "button",
      "cssVar": "--button-border-width-ghost",
      "path": [
        "border",
        "width",
        "ghost"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-hairline)"
    },
    {
      "family": "button",
      "cssVar": "--button-border-width-focus",
      "path": [
        "border",
        "width",
        "focus"
      ],
      "$type": "dimension",
      "$value": "var(--border-width-default)"
    },
    {
      "family": "button",
      "cssVar": "--button-block-size-s",
      "path": [
        "block",
        "size",
        "s"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "button",
      "cssVar": "--button-block-size-m",
      "path": [
        "block",
        "size",
        "m"
      ],
      "$type": "dimension",
      "$value": "36px"
    },
    {
      "family": "button",
      "cssVar": "--button-block-size-l",
      "path": [
        "block",
        "size",
        "l"
      ],
      "$type": "dimension",
      "$value": "40px"
    },
    {
      "family": "button",
      "cssVar": "--button-block-size-xl",
      "path": [
        "block",
        "size",
        "xl"
      ],
      "$type": "dimension",
      "$value": "48px"
    },
    {
      "family": "button",
      "cssVar": "--button-min-inline-size",
      "path": [
        "min",
        "inline",
        "size"
      ],
      "$type": "dimension",
      "$value": "48px"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-block-s",
      "path": [
        "padding",
        "block",
        "s"
      ],
      "$type": "dimension",
      "$value": "var(--space-3)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-block-m",
      "path": [
        "padding",
        "block",
        "m"
      ],
      "$type": "dimension",
      "$value": "var(--space-3)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-block-l",
      "path": [
        "padding",
        "block",
        "l"
      ],
      "$type": "dimension",
      "$value": "var(--space-3)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-block-xl",
      "path": [
        "padding",
        "block",
        "xl"
      ],
      "$type": "dimension",
      "$value": "var(--space-4)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-inline-s",
      "path": [
        "padding",
        "inline",
        "s"
      ],
      "$type": "dimension",
      "$value": "var(--space-3)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-inline-m",
      "path": [
        "padding",
        "inline",
        "m"
      ],
      "$type": "dimension",
      "$value": "var(--space-4)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-inline-l",
      "path": [
        "padding",
        "inline",
        "l"
      ],
      "$type": "dimension",
      "$value": "var(--space-5)"
    },
    {
      "family": "button",
      "cssVar": "--button-padding-inline-xl",
      "path": [
        "padding",
        "inline",
        "xl"
      ],
      "$type": "dimension",
      "$value": "var(--space-7)"
    },
    {
      "family": "button",
      "cssVar": "--button-gap",
      "path": [
        "gap"
      ],
      "$type": "dimension",
      "$value": "var(--space-3)"
    },
    {
      "family": "button",
      "cssVar": "--button-icon-size-s",
      "path": [
        "icon",
        "size",
        "s"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "button",
      "cssVar": "--button-icon-size-m",
      "path": [
        "icon",
        "size",
        "m"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "button",
      "cssVar": "--button-icon-size-l",
      "path": [
        "icon",
        "size",
        "l"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "button",
      "cssVar": "--button-icon-size-xl",
      "path": [
        "icon",
        "size",
        "xl"
      ],
      "$type": "dimension",
      "$value": "20px"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/colors

---
type: token
name: colors
status: published
---

# Colors

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--color-avg-black` | `#1c222e` | |
| `--color-avg-white` | `#ffffff` | |
| `--color-avg-green` | `#008941` | |
| `--color-avg-blue` | `#2276d9` | |
| `--color-avg-green-inverse` | `#24aa63` | |
| `--color-avg-mid-green` | `#7acca0` | |
| `--color-avg-light-green` | `#a2dbbd` | |
| `--color-avg-pale-green` | `#d1edde` | |
| `--color-avg-faint-green` | `#e6f4ed` | |
| `--color-avg-off-white-green` | `#f2faf6` | |
| `--color-avg-alt-green` | `#00783b` | |
| `--color-avg-blue-inverse` | `#4d99f0` | |
| `--color-avg-mid-blue` | `#93c1f5` | |
| `--color-avg-light-blue` | `#b6d5f7` | |
| `--color-avg-pale-blue` | `#d9e8fa` | |
| `--color-avg-faint-blue` | `#e9f1fb` | |
| `--color-avg-off-white-blue` | `#f2f8ff` | |
| `--color-avg-alt-blue` | `#1d67bf` | |
| `--color-avg-red` | `#db3559` | |
| `--color-avg-yellow` | `#ffb600` | |
| `--color-avg-pale-yellow` | `#ffe29e` | |
| `--color-avg-faint-yellow` | `#fff2d4` | |
| `--color-avg-off-white-yellow` | `#fff9ed` | |
| `--color-avg-grey-1` | `#2b323f` | |
| `--color-avg-grey-2` | `#3a4252` | |
| `--color-avg-grey-3` | `#697284` | |
| `--color-avg-grey-4` | `#9aa3b5` | |
| `--color-avg-grey-5` | `#ced4e0` | |
| `--color-avg-grey-6` | `#e4e8f0` | |
| `--color-avg-grey-7` | `#edf0f7` | |
| `--color-avg-grey-8` | `#f7f9fc` | |
| `--color-avg-critical` | `#db3559` | |
| `--color-avg-attention` | `#ffb600` | |
| `--color-avg-success` | `#00783b` | |
| `--color-avg-info` | `#1d67bf` | |
| `--color-avg-neutral-50` | `#f4f4f5` | |
| `--color-avg-neutral-100` | `#e8e9ea` | |
| `--color-avg-neutral-200` | `#d2d3d5` | |
| `--color-avg-neutral-300` | `#bbbdc1` | |
| `--color-avg-neutral-400` | `#a4a7ac` | |
| `--color-avg-neutral-500` | `#8e9197` | |
| `--color-avg-neutral-600` | `#777a83` | |
| `--color-avg-neutral-700` | `#60646e` | |
| `--color-avg-neutral-800` | `#494e59` | |
| `--color-avg-neutral-900` | `#333844` | |
| `--color-white` | `var(--color-avg-white)` | |
| `--color-functional-critical` | `var(--color-avg-critical)` | |
| `--color-functional-critical-muted` | `#eb98a9` | |
| `--color-functional-critical-subtle` | `#f7e6e9` | |
| `--color-functional-attention` | `var(--color-avg-attention)` | |
| `--color-functional-attention-muted` | `#fdd87c` | |
| `--color-functional-attention-subtle` | `#fbf3e0` | |
| `--color-functional-success` | `var(--color-avg-success)` | |
| `--color-functional-success-muted` | `#7db99a` | |
| `--color-functional-success-subtle` | `#e1ede6` | |
| `--color-functional-info` | `var(--color-avg-info)` | |
| `--color-functional-info-muted` | `#8cb1dc` | |
| `--color-functional-info-subtle` | `#e4ebf3` | |
| `--color-neutral-5` | `var(--color-avg-neutral-50)` | |
| `--color-neutral-10` | `var(--color-avg-neutral-100)` | |
| `--color-neutral-20` | `var(--color-avg-neutral-200)` | |
| `--color-neutral-30` | `var(--color-avg-neutral-300)` | |
| `--color-neutral-40` | `var(--color-avg-neutral-400)` | |
| `--color-neutral-50` | `var(--color-avg-neutral-500)` | |
| `--color-neutral-60` | `var(--color-avg-neutral-600)` | |
| `--color-neutral-70` | `var(--color-avg-neutral-700)` | |
| `--color-neutral-80` | `var(--color-avg-neutral-800)` | |
| `--color-neutral-90` | `var(--color-avg-neutral-900)` | |
| `--color-bg-default` | `var(--color-avg-white)` | |
| `--color-bg-subtle` | `var(--color-avg-off-white-green)` | |
| `--color-bg-muted` | `var(--color-avg-off-white-blue)` | |
| `--color-bg-brand` | `var(--color-avg-green)` | |
| `--color-bg-brand-soft` | `var(--color-avg-faint-green)` | |
| `--color-bg-accent` | `var(--color-avg-pale-yellow)` | |
| `--color-bg-alpha` | `var(--color-avg-light-blue)` | |
| `--color-bg-beta` | `var(--color-avg-light-green)` | |
| `--color-bg-gamma` | `var(--color-avg-off-white-yellow)` | |
| `--color-bg-delta` | `var(--color-avg-grey-7)` | |
| `--color-bg-inverse` | `var(--color-avg-black)` | |
| `--color-bg-inverse-strong` | `var(--color-avg-black)` | |
| `--color-bg-primary` | `var(--color-bg-default)` | |
| `--color-text-primary` | `var(--color-avg-black)` | |
| `--color-text-secondary` | `var(--color-avg-grey-3)` | |
| `--color-text-brand` | `var(--color-avg-green)` | |
| `--color-text-accent` | `var(--color-avg-blue)` | |
| `--color-text-inverse` | `var(--color-avg-white)` | |
| `--color-border-strong` | `var(--color-avg-black)` | |
| `--color-border-strong-alt` | `var(--color-avg-grey-2)` | |
| `--color-border-subtle` | `var(--color-avg-neutral-100)` | |
| `--color-border-inverse` | `var(--color-avg-white)` | |
| `--color-border-brand` | `var(--color-avg-green)` | |
| `--color-border-accent` | `var(--color-avg-blue)` | |
| `--color-border-focus` | `var(--color-avg-green)` | |
| `--color-highlight-brand` | `var(--color-avg-blue)` | |
| `--color-highlight-brand-light` | `var(--color-avg-pale-green)` | |
| `--color-highlight-accent` | `var(--color-avg-green)` | |
| `--color-highlight-alpha-base` | `var(--color-avg-red)` | |
| `--color-highlight-beta-base` | `var(--color-avg-grey-3)` | |
| `--color-highlight-gamma-base` | `var(--color-avg-yellow)` | |
| `--color-highlight-delta-base` | `var(--color-avg-pale-yellow)` | |
| `--color-highlight-epsilon-base` | `var(--color-avg-mid-green)` | |
| `--color-highlight-zeta-base` | `var(--color-avg-mid-blue)` | |
| `--color-highlight-eta-base` | `var(--color-avg-blue-inverse)` | |
| `--color-highlight-theta-base` | `var(--color-avg-green-inverse)` | |
| `--color-highlight-iota-base` | `var(--color-avg-grey-3)` | |
| `--color-signal-critical` | `var(--color-functional-critical)` | |
| `--color-signal-critical-muted` | `var(--color-functional-critical-muted)` | |
| `--color-signal-critical-subtle` | `var(--color-functional-critical-subtle)` | |
| `--color-signal-warning` | `var(--color-functional-attention)` | |
| `--color-signal-warning-muted` | `var(--color-functional-attention-muted)` | |
| `--color-signal-warning-subtle` | `var(--color-functional-attention-subtle)` | |
| `--color-signal-success` | `var(--color-functional-success)` | |
| `--color-signal-success-muted` | `var(--color-functional-success-muted)` | |
| `--color-signal-success-subtle` | `var(--color-functional-success-subtle)` | |
| `--color-signal-info` | `var(--color-functional-info)` | |
| `--color-signal-info-muted` | `var(--color-functional-info-muted)` | |
| `--color-signal-info-subtle` | `var(--color-functional-info-subtle)` | |
| `--color-disabled-text` | `var(--color-neutral-50)` | |
| `--color-disabled-border` | `var(--color-neutral-30)` | |
| `--color-disabled-bg` | `var(--color-neutral-20)` | |
| `--color-canvas-default` | `var(--color-avg-white)` | |
| `--color-canvas-subtle` | `var(--color-avg-grey-8)` | |
| `--color-canvas-contrast` | `var(--color-avg-grey-8)` | |
| `--color-shadow-default` | `var(--color-neutral-50)` | |
| `--gradient-horizontal` | `linear-gradient(90deg, var(--color-avg-green) 0%, var(--color-avg-blue) 100%)` | |
| `--gradient-vertical` | `linear-gradient(180deg, var(--color-avg-green) 0%, var(--color-avg-blue) 100%)` | |


### colors.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "colors",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/colors/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/colors/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "colors",
      "cssVar": "--color-avg-black",
      "path": [
        "avg",
        "black"
      ],
      "$type": "color",
      "$value": "#1c222e"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-white",
      "path": [
        "avg",
        "white"
      ],
      "$type": "color",
      "$value": "#ffffff"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-green",
      "path": [
        "avg",
        "green"
      ],
      "$type": "color",
      "$value": "#008941"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-blue",
      "path": [
        "avg",
        "blue"
      ],
      "$type": "color",
      "$value": "#2276d9"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-green-inverse",
      "path": [
        "avg",
        "green",
        "inverse"
      ],
      "$type": "color",
      "$value": "#24aa63"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-mid-green",
      "path": [
        "avg",
        "mid",
        "green"
      ],
      "$type": "color",
      "$value": "#7acca0"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-light-green",
      "path": [
        "avg",
        "light",
        "green"
      ],
      "$type": "color",
      "$value": "#a2dbbd"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-pale-green",
      "path": [
        "avg",
        "pale",
        "green"
      ],
      "$type": "color",
      "$value": "#d1edde"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-faint-green",
      "path": [
        "avg",
        "faint",
        "green"
      ],
      "$type": "color",
      "$value": "#e6f4ed"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-off-white-green",
      "path": [
        "avg",
        "off",
        "white",
        "green"
      ],
      "$type": "color",
      "$value": "#f2faf6"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-alt-green",
      "path": [
        "avg",
        "alt",
        "green"
      ],
      "$type": "color",
      "$value": "#00783b"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-blue-inverse",
      "path": [
        "avg",
        "blue",
        "inverse"
      ],
      "$type": "color",
      "$value": "#4d99f0"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-mid-blue",
      "path": [
        "avg",
        "mid",
        "blue"
      ],
      "$type": "color",
      "$value": "#93c1f5"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-light-blue",
      "path": [
        "avg",
        "light",
        "blue"
      ],
      "$type": "color",
      "$value": "#b6d5f7"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-pale-blue",
      "path": [
        "avg",
        "pale",
        "blue"
      ],
      "$type": "color",
      "$value": "#d9e8fa"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-faint-blue",
      "path": [
        "avg",
        "faint",
        "blue"
      ],
      "$type": "color",
      "$value": "#e9f1fb"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-off-white-blue",
      "path": [
        "avg",
        "off",
        "white",
        "blue"
      ],
      "$type": "color",
      "$value": "#f2f8ff"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-alt-blue",
      "path": [
        "avg",
        "alt",
        "blue"
      ],
      "$type": "color",
      "$value": "#1d67bf"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-red",
      "path": [
        "avg",
        "red"
      ],
      "$type": "color",
      "$value": "#db3559"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-yellow",
      "path": [
        "avg",
        "yellow"
      ],
      "$type": "color",
      "$value": "#ffb600"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-pale-yellow",
      "path": [
        "avg",
        "pale",
        "yellow"
      ],
      "$type": "color",
      "$value": "#ffe29e"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-faint-yellow",
      "path": [
        "avg",
        "faint",
        "yellow"
      ],
      "$type": "color",
      "$value": "#fff2d4"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-off-white-yellow",
      "path": [
        "avg",
        "off",
        "white",
        "yellow"
      ],
      "$type": "color",
      "$value": "#fff9ed"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-1",
      "path": [
        "avg",
        "grey",
        "1"
      ],
      "$type": "color",
      "$value": "#2b323f"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-2",
      "path": [
        "avg",
        "grey",
        "2"
      ],
      "$type": "color",
      "$value": "#3a4252"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-3",
      "path": [
        "avg",
        "grey",
        "3"
      ],
      "$type": "color",
      "$value": "#697284"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-4",
      "path": [
        "avg",
        "grey",
        "4"
      ],
      "$type": "color",
      "$value": "#9aa3b5"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-5",
      "path": [
        "avg",
        "grey",
        "5"
      ],
      "$type": "color",
      "$value": "#ced4e0"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-6",
      "path": [
        "avg",
        "grey",
        "6"
      ],
      "$type": "color",
      "$value": "#e4e8f0"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-7",
      "path": [
        "avg",
        "grey",
        "7"
      ],
      "$type": "color",
      "$value": "#edf0f7"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-grey-8",
      "path": [
        "avg",
        "grey",
        "8"
      ],
      "$type": "color",
      "$value": "#f7f9fc"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-critical",
      "path": [
        "avg",
        "critical"
      ],
      "$type": "color",
      "$value": "#db3559"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-attention",
      "path": [
        "avg",
        "attention"
      ],
      "$type": "color",
      "$value": "#ffb600"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-success",
      "path": [
        "avg",
        "success"
      ],
      "$type": "color",
      "$value": "#00783b"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-info",
      "path": [
        "avg",
        "info"
      ],
      "$type": "color",
      "$value": "#1d67bf"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-50",
      "path": [
        "avg",
        "neutral",
        "50"
      ],
      "$type": "color",
      "$value": "#f4f4f5"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-100",
      "path": [
        "avg",
        "neutral",
        "100"
      ],
      "$type": "color",
      "$value": "#e8e9ea"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-200",
      "path": [
        "avg",
        "neutral",
        "200"
      ],
      "$type": "color",
      "$value": "#d2d3d5"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-300",
      "path": [
        "avg",
        "neutral",
        "300"
      ],
      "$type": "color",
      "$value": "#bbbdc1"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-400",
      "path": [
        "avg",
        "neutral",
        "400"
      ],
      "$type": "color",
      "$value": "#a4a7ac"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-500",
      "path": [
        "avg",
        "neutral",
        "500"
      ],
      "$type": "color",
      "$value": "#8e9197"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-600",
      "path": [
        "avg",
        "neutral",
        "600"
      ],
      "$type": "color",
      "$value": "#777a83"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-700",
      "path": [
        "avg",
        "neutral",
        "700"
      ],
      "$type": "color",
      "$value": "#60646e"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-800",
      "path": [
        "avg",
        "neutral",
        "800"
      ],
      "$type": "color",
      "$value": "#494e59"
    },
    {
      "family": "colors",
      "cssVar": "--color-avg-neutral-900",
      "path": [
        "avg",
        "neutral",
        "900"
      ],
      "$type": "color",
      "$value": "#333844"
    },
    {
      "family": "colors",
      "cssVar": "--color-white",
      "path": [
        "white"
      ],
      "$type": "color",
      "$value": "var(--color-avg-white)"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-critical",
      "path": [
        "functional",
        "critical"
      ],
      "$type": "color",
      "$value": "var(--color-avg-critical)"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-critical-muted",
      "path": [
        "functional",
        "critical",
        "muted"
      ],
      "$type": "color",
      "$value": "#eb98a9"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-critical-subtle",
      "path": [
        "functional",
        "critical",
        "subtle"
      ],
      "$type": "color",
      "$value": "#f7e6e9"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-attention",
      "path": [
        "functional",
        "attention"
      ],
      "$type": "color",
      "$value": "var(--color-avg-attention)"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-attention-muted",
      "path": [
        "functional",
        "attention",
        "muted"
      ],
      "$type": "color",
      "$value": "#fdd87c"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-attention-subtle",
      "path": [
        "functional",
        "attention",
        "subtle"
      ],
      "$type": "color",
      "$value": "#fbf3e0"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-success",
      "path": [
        "functional",
        "success"
      ],
      "$type": "color",
      "$value": "var(--color-avg-success)"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-success-muted",
      "path": [
        "functional",
        "success",
        "muted"
      ],
      "$type": "color",
      "$value": "#7db99a"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-success-subtle",
      "path": [
        "functional",
        "success",
        "subtle"
      ],
      "$type": "color",
      "$value": "#e1ede6"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-info",
      "path": [
        "functional",
        "info"
      ],
      "$type": "color",
      "$value": "var(--color-avg-info)"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-info-muted",
      "path": [
        "functional",
        "info",
        "muted"
      ],
      "$type": "color",
      "$value": "#8cb1dc"
    },
    {
      "family": "colors",
      "cssVar": "--color-functional-info-subtle",
      "path": [
        "functional",
        "info",
        "subtle"
      ],
      "$type": "color",
      "$value": "#e4ebf3"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-5",
      "path": [
        "neutral",
        "5"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-50)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-10",
      "path": [
        "neutral",
        "10"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-100)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-20",
      "path": [
        "neutral",
        "20"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-200)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-30",
      "path": [
        "neutral",
        "30"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-300)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-40",
      "path": [
        "neutral",
        "40"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-400)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-50",
      "path": [
        "neutral",
        "50"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-500)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-60",
      "path": [
        "neutral",
        "60"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-600)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-70",
      "path": [
        "neutral",
        "70"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-700)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-80",
      "path": [
        "neutral",
        "80"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-800)"
    },
    {
      "family": "colors",
      "cssVar": "--color-neutral-90",
      "path": [
        "neutral",
        "90"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-900)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-default",
      "path": [
        "bg",
        "default"
      ],
      "$type": "color",
      "$value": "var(--color-avg-white)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-subtle",
      "path": [
        "bg",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-avg-off-white-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-muted",
      "path": [
        "bg",
        "muted"
      ],
      "$type": "color",
      "$value": "var(--color-avg-off-white-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-brand",
      "path": [
        "bg",
        "brand"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-brand-soft",
      "path": [
        "bg",
        "brand",
        "soft"
      ],
      "$type": "color",
      "$value": "var(--color-avg-faint-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-accent",
      "path": [
        "bg",
        "accent"
      ],
      "$type": "color",
      "$value": "var(--color-avg-pale-yellow)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-alpha",
      "path": [
        "bg",
        "alpha"
      ],
      "$type": "color",
      "$value": "var(--color-avg-light-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-beta",
      "path": [
        "bg",
        "beta"
      ],
      "$type": "color",
      "$value": "var(--color-avg-light-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-gamma",
      "path": [
        "bg",
        "gamma"
      ],
      "$type": "color",
      "$value": "var(--color-avg-off-white-yellow)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-delta",
      "path": [
        "bg",
        "delta"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-7)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-inverse",
      "path": [
        "bg",
        "inverse"
      ],
      "$type": "color",
      "$value": "var(--color-avg-black)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-inverse-strong",
      "path": [
        "bg",
        "inverse",
        "strong"
      ],
      "$type": "color",
      "$value": "var(--color-avg-black)"
    },
    {
      "family": "colors",
      "cssVar": "--color-bg-primary",
      "path": [
        "bg",
        "primary"
      ],
      "$type": "color",
      "$value": "var(--color-bg-default)"
    },
    {
      "family": "colors",
      "cssVar": "--color-text-primary",
      "path": [
        "text",
        "primary"
      ],
      "$type": "color",
      "$value": "var(--color-avg-black)"
    },
    {
      "family": "colors",
      "cssVar": "--color-text-secondary",
      "path": [
        "text",
        "secondary"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-3)"
    },
    {
      "family": "colors",
      "cssVar": "--color-text-brand",
      "path": [
        "text",
        "brand"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-text-accent",
      "path": [
        "text",
        "accent"
      ],
      "$type": "color",
      "$value": "var(--color-avg-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-text-inverse",
      "path": [
        "text",
        "inverse"
      ],
      "$type": "color",
      "$value": "var(--color-avg-white)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-strong",
      "path": [
        "border",
        "strong"
      ],
      "$type": "color",
      "$value": "var(--color-avg-black)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-strong-alt",
      "path": [
        "border",
        "strong",
        "alt"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-2)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-subtle",
      "path": [
        "border",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-avg-neutral-100)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-inverse",
      "path": [
        "border",
        "inverse"
      ],
      "$type": "color",
      "$value": "var(--color-avg-white)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-brand",
      "path": [
        "border",
        "brand"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-accent",
      "path": [
        "border",
        "accent"
      ],
      "$type": "color",
      "$value": "var(--color-avg-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-border-focus",
      "path": [
        "border",
        "focus"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-brand",
      "path": [
        "highlight",
        "brand"
      ],
      "$type": "color",
      "$value": "var(--color-avg-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-brand-light",
      "path": [
        "highlight",
        "brand",
        "light"
      ],
      "$type": "color",
      "$value": "var(--color-avg-pale-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-accent",
      "path": [
        "highlight",
        "accent"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-alpha-base",
      "path": [
        "highlight",
        "alpha",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-red)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-beta-base",
      "path": [
        "highlight",
        "beta",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-3)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-gamma-base",
      "path": [
        "highlight",
        "gamma",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-yellow)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-delta-base",
      "path": [
        "highlight",
        "delta",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-pale-yellow)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-epsilon-base",
      "path": [
        "highlight",
        "epsilon",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-mid-green)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-zeta-base",
      "path": [
        "highlight",
        "zeta",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-mid-blue)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-eta-base",
      "path": [
        "highlight",
        "eta",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-blue-inverse)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-theta-base",
      "path": [
        "highlight",
        "theta",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-green-inverse)"
    },
    {
      "family": "colors",
      "cssVar": "--color-highlight-iota-base",
      "path": [
        "highlight",
        "iota",
        "base"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-3)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-critical",
      "path": [
        "signal",
        "critical"
      ],
      "$type": "color",
      "$value": "var(--color-functional-critical)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-critical-muted",
      "path": [
        "signal",
        "critical",
        "muted"
      ],
      "$type": "color",
      "$value": "var(--color-functional-critical-muted)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-critical-subtle",
      "path": [
        "signal",
        "critical",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-functional-critical-subtle)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-warning",
      "path": [
        "signal",
        "warning"
      ],
      "$type": "color",
      "$value": "var(--color-functional-attention)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-warning-muted",
      "path": [
        "signal",
        "warning",
        "muted"
      ],
      "$type": "color",
      "$value": "var(--color-functional-attention-muted)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-warning-subtle",
      "path": [
        "signal",
        "warning",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-functional-attention-subtle)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-success",
      "path": [
        "signal",
        "success"
      ],
      "$type": "color",
      "$value": "var(--color-functional-success)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-success-muted",
      "path": [
        "signal",
        "success",
        "muted"
      ],
      "$type": "color",
      "$value": "var(--color-functional-success-muted)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-success-subtle",
      "path": [
        "signal",
        "success",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-functional-success-subtle)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-info",
      "path": [
        "signal",
        "info"
      ],
      "$type": "color",
      "$value": "var(--color-functional-info)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-info-muted",
      "path": [
        "signal",
        "info",
        "muted"
      ],
      "$type": "color",
      "$value": "var(--color-functional-info-muted)"
    },
    {
      "family": "colors",
      "cssVar": "--color-signal-info-subtle",
      "path": [
        "signal",
        "info",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-functional-info-subtle)"
    },
    {
      "family": "colors",
      "cssVar": "--color-disabled-text",
      "path": [
        "disabled",
        "text"
      ],
      "$type": "color",
      "$value": "var(--color-neutral-50)"
    },
    {
      "family": "colors",
      "cssVar": "--color-disabled-border",
      "path": [
        "disabled",
        "border"
      ],
      "$type": "color",
      "$value": "var(--color-neutral-30)"
    },
    {
      "family": "colors",
      "cssVar": "--color-disabled-bg",
      "path": [
        "disabled",
        "bg"
      ],
      "$type": "color",
      "$value": "var(--color-neutral-20)"
    },
    {
      "family": "colors",
      "cssVar": "--color-canvas-default",
      "path": [
        "canvas",
        "default"
      ],
      "$type": "color",
      "$value": "var(--color-avg-white)"
    },
    {
      "family": "colors",
      "cssVar": "--color-canvas-subtle",
      "path": [
        "canvas",
        "subtle"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-8)"
    },
    {
      "family": "colors",
      "cssVar": "--color-canvas-contrast",
      "path": [
        "canvas",
        "contrast"
      ],
      "$type": "color",
      "$value": "var(--color-avg-grey-8)"
    },
    {
      "family": "colors",
      "cssVar": "--color-shadow-default",
      "path": [
        "shadow",
        "default"
      ],
      "$type": "color",
      "$value": "var(--color-neutral-50)"
    },
    {
      "family": "colors",
      "cssVar": "--gradient-horizontal",
      "path": [
        "gradient",
        "horizontal"
      ],
      "$type": "color",
      "$value": "linear-gradient(90deg, var(--color-avg-green) 0%, var(--color-avg-blue) 100%)"
    },
    {
      "family": "colors",
      "cssVar": "--gradient-vertical",
      "path": [
        "gradient",
        "vertical"
      ],
      "$type": "color",
      "$value": "linear-gradient(180deg, var(--color-avg-green) 0%, var(--color-avg-blue) 100%)"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/grid

---
type: token
name: grid
status: published
---

# Grid

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--breakpoint-sm` | `360px` | |
| `--breakpoint-md` | `768px` | |
| `--breakpoint-lg` | `1024px` | |
| `--breakpoint-xl` | `1440px` | |
| `--grid-content-sm` | `328px` | |
| `--grid-content-md` | `704px` | |
| `--grid-content-lg` | `936px` | |
| `--grid-content-xl` | `1312px` | |
| `--grid-max-width` | `var(--grid-content-xl)` | |
| `--grid-columns-sm` | `4` | |
| `--grid-columns-md` | `12` | |
| `--grid-columns-lg` | `12` | |
| `--grid-columns-xl` | `12` | |
| `--grid-gutter-sm` | `12px` | |
| `--grid-gutter-md` | `16px` | |
| `--grid-gutter-lg` | `24px` | |
| `--grid-gutter-xl` | `32px` | |
| `--grid-margin-sm` | `16px` | |
| `--grid-margin-md` | `32px` | |
| `--grid-margin-lg` | `44px` | |
| `--grid-margin-xl` | `64px` | |
| `--grid-column-width-sm` | `73px` | |
| `--grid-column-width-md` | `44px` | |
| `--grid-column-width-lg` | `56px` | |
| `--grid-column-width-xl` | `80px` | |


### grid.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "grid",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/grid/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/grid/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "grid",
      "cssVar": "--breakpoint-sm",
      "path": [
        "breakpoint",
        "sm"
      ],
      "$type": "dimension",
      "$value": "360px"
    },
    {
      "family": "grid",
      "cssVar": "--breakpoint-md",
      "path": [
        "breakpoint",
        "md"
      ],
      "$type": "dimension",
      "$value": "768px"
    },
    {
      "family": "grid",
      "cssVar": "--breakpoint-lg",
      "path": [
        "breakpoint",
        "lg"
      ],
      "$type": "dimension",
      "$value": "1024px"
    },
    {
      "family": "grid",
      "cssVar": "--breakpoint-xl",
      "path": [
        "breakpoint",
        "xl"
      ],
      "$type": "dimension",
      "$value": "1440px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-content-sm",
      "path": [
        "content",
        "sm"
      ],
      "$type": "dimension",
      "$value": "328px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-content-md",
      "path": [
        "content",
        "md"
      ],
      "$type": "dimension",
      "$value": "704px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-content-lg",
      "path": [
        "content",
        "lg"
      ],
      "$type": "dimension",
      "$value": "936px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-content-xl",
      "path": [
        "content",
        "xl"
      ],
      "$type": "dimension",
      "$value": "1312px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-max-width",
      "path": [
        "max",
        "width"
      ],
      "$type": "dimension",
      "$value": "var(--grid-content-xl)"
    },
    {
      "family": "grid",
      "cssVar": "--grid-columns-sm",
      "path": [
        "columns",
        "sm"
      ],
      "$type": "dimension",
      "$value": "4"
    },
    {
      "family": "grid",
      "cssVar": "--grid-columns-md",
      "path": [
        "columns",
        "md"
      ],
      "$type": "dimension",
      "$value": "12"
    },
    {
      "family": "grid",
      "cssVar": "--grid-columns-lg",
      "path": [
        "columns",
        "lg"
      ],
      "$type": "dimension",
      "$value": "12"
    },
    {
      "family": "grid",
      "cssVar": "--grid-columns-xl",
      "path": [
        "columns",
        "xl"
      ],
      "$type": "dimension",
      "$value": "12"
    },
    {
      "family": "grid",
      "cssVar": "--grid-gutter-sm",
      "path": [
        "gutter",
        "sm"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-gutter-md",
      "path": [
        "gutter",
        "md"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-gutter-lg",
      "path": [
        "gutter",
        "lg"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-gutter-xl",
      "path": [
        "gutter",
        "xl"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-margin-sm",
      "path": [
        "margin",
        "sm"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-margin-md",
      "path": [
        "margin",
        "md"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-margin-lg",
      "path": [
        "margin",
        "lg"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-margin-xl",
      "path": [
        "margin",
        "xl"
      ],
      "$type": "dimension",
      "$value": "64px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-column-width-sm",
      "path": [
        "column",
        "width",
        "sm"
      ],
      "$type": "dimension",
      "$value": "73px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-column-width-md",
      "path": [
        "column",
        "width",
        "md"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-column-width-lg",
      "path": [
        "column",
        "width",
        "lg"
      ],
      "$type": "dimension",
      "$value": "56px"
    },
    {
      "family": "grid",
      "cssVar": "--grid-column-width-xl",
      "path": [
        "column",
        "width",
        "xl"
      ],
      "$type": "dimension",
      "$value": "80px"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/opacity

---
type: token
name: opacity
status: published
---

# Opacity

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--opacity-0` | `0%` | |
| `--opacity-5` | `5%` | |
| `--opacity-10` | `10%` | |
| `--opacity-20` | `20%` | |
| `--opacity-25` | `25%` | |
| `--opacity-30` | `30%` | |
| `--opacity-40` | `40%` | |
| `--opacity-50` | `50%` | |
| `--opacity-60` | `60%` | |
| `--opacity-70` | `70%` | |
| `--opacity-75` | `75%` | |
| `--opacity-80` | `80%` | |
| `--opacity-90` | `90%` | |


### opacity.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "opacity",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/opacity/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/opacity/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "opacity",
      "cssVar": "--opacity-0",
      "path": [
        "0"
      ],
      "$type": "number",
      "$value": "0%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-5",
      "path": [
        "5"
      ],
      "$type": "number",
      "$value": "5%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-10",
      "path": [
        "10"
      ],
      "$type": "number",
      "$value": "10%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-20",
      "path": [
        "20"
      ],
      "$type": "number",
      "$value": "20%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-25",
      "path": [
        "25"
      ],
      "$type": "number",
      "$value": "25%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-30",
      "path": [
        "30"
      ],
      "$type": "number",
      "$value": "30%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-40",
      "path": [
        "40"
      ],
      "$type": "number",
      "$value": "40%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-50",
      "path": [
        "50"
      ],
      "$type": "number",
      "$value": "50%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-60",
      "path": [
        "60"
      ],
      "$type": "number",
      "$value": "60%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-70",
      "path": [
        "70"
      ],
      "$type": "number",
      "$value": "70%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-75",
      "path": [
        "75"
      ],
      "$type": "number",
      "$value": "75%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-80",
      "path": [
        "80"
      ],
      "$type": "number",
      "$value": "80%"
    },
    {
      "family": "opacity",
      "cssVar": "--opacity-90",
      "path": [
        "90"
      ],
      "$type": "number",
      "$value": "90%"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/shadow

---
type: token
name: shadow
status: published
---

# Shadow

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--shadow-raw-x-default` | `0` | |
| `--shadow-raw-y-default` | `6px` | |
| `--shadow-raw-blur-default` | `24px` | |
| `--shadow-raw-spread-default` | `0` | |
| `--shadow-raw-x-hover` | `0` | |
| `--shadow-raw-y-hover` | `6px` | |
| `--shadow-raw-blur-hover` | `24px` | |
| `--shadow-raw-spread-hover` | `0` | |
| `--shadow-raw-x-pressed` | `0` | |
| `--shadow-raw-y-pressed` | `2px` | |
| `--shadow-raw-blur-pressed` | `6px` | |
| `--shadow-raw-spread-pressed` | `0` | |
| `--shadow-default` | `0 6px 24px 0 rgb(0 0 0 / var(--opacity-10))` | |
| `--shadow-hover` | `0 6px 24px 0 rgb(0 0 0 / var(--opacity-10))` | |
| `--shadow-pressed` | `0 2px 6px 0 rgb(0 0 0 / var(--opacity-10))` | |


### shadow.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "shadow",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/shadow/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/shadow/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-x-default",
      "path": [
        "raw",
        "x",
        "default"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-y-default",
      "path": [
        "raw",
        "y",
        "default"
      ],
      "$type": "dimension",
      "$value": "6px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-blur-default",
      "path": [
        "raw",
        "blur",
        "default"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-spread-default",
      "path": [
        "raw",
        "spread",
        "default"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-x-hover",
      "path": [
        "raw",
        "x",
        "hover"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-y-hover",
      "path": [
        "raw",
        "y",
        "hover"
      ],
      "$type": "dimension",
      "$value": "6px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-blur-hover",
      "path": [
        "raw",
        "blur",
        "hover"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-spread-hover",
      "path": [
        "raw",
        "spread",
        "hover"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-x-pressed",
      "path": [
        "raw",
        "x",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-y-pressed",
      "path": [
        "raw",
        "y",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "2px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-blur-pressed",
      "path": [
        "raw",
        "blur",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "6px"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-raw-spread-pressed",
      "path": [
        "raw",
        "spread",
        "pressed"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-default",
      "path": [
        "default"
      ],
      "$type": "dimension",
      "$value": "0 6px 24px 0 rgb(0 0 0 / var(--opacity-10))"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-hover",
      "path": [
        "hover"
      ],
      "$type": "dimension",
      "$value": "0 6px 24px 0 rgb(0 0 0 / var(--opacity-10))"
    },
    {
      "family": "shadow",
      "cssVar": "--shadow-pressed",
      "path": [
        "pressed"
      ],
      "$type": "dimension",
      "$value": "0 2px 6px 0 rgb(0 0 0 / var(--opacity-10))"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/spacing

---
type: token
name: spacing
status: published
---

# Spacing

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--space-0` | `0` | |
| `--space-1` | `2px` | |
| `--space-2` | `4px` | |
| `--space-3` | `8px` | |
| `--space-4` | `12px` | |
| `--space-5` | `16px` | |
| `--space-6` | `20px` | |
| `--space-7` | `24px` | |
| `--space-8` | `32px` | |
| `--space-9` | `40px` | |
| `--space-10` | `44px` | |
| `--space-11` | `48px` | |
| `--space-12` | `56px` | |
| `--space-13` | `64px` | |
| `--space-14` | `72px` | |
| `--space-15` | `80px` | |
| `--space-16` | `88px` | |
| `--space-17` | `96px` | |
| `--space-18` | `104px` | |
| `--space-19` | `112px` | |
| `--space-20` | `120px` | |


### spacing.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "spacing",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/spacing/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/spacing/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "spacing",
      "cssVar": "--space-0",
      "path": [
        "0"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "spacing",
      "cssVar": "--space-1",
      "path": [
        "1"
      ],
      "$type": "dimension",
      "$value": "2px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-2",
      "path": [
        "2"
      ],
      "$type": "dimension",
      "$value": "4px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-3",
      "path": [
        "3"
      ],
      "$type": "dimension",
      "$value": "8px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-4",
      "path": [
        "4"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-5",
      "path": [
        "5"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-6",
      "path": [
        "6"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-7",
      "path": [
        "7"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-8",
      "path": [
        "8"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-9",
      "path": [
        "9"
      ],
      "$type": "dimension",
      "$value": "40px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-10",
      "path": [
        "10"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-11",
      "path": [
        "11"
      ],
      "$type": "dimension",
      "$value": "48px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-12",
      "path": [
        "12"
      ],
      "$type": "dimension",
      "$value": "56px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-13",
      "path": [
        "13"
      ],
      "$type": "dimension",
      "$value": "64px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-14",
      "path": [
        "14"
      ],
      "$type": "dimension",
      "$value": "72px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-15",
      "path": [
        "15"
      ],
      "$type": "dimension",
      "$value": "80px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-16",
      "path": [
        "16"
      ],
      "$type": "dimension",
      "$value": "88px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-17",
      "path": [
        "17"
      ],
      "$type": "dimension",
      "$value": "96px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-18",
      "path": [
        "18"
      ],
      "$type": "dimension",
      "$value": "104px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-19",
      "path": [
        "19"
      ],
      "$type": "dimension",
      "$value": "112px"
    },
    {
      "family": "spacing",
      "cssVar": "--space-20",
      "path": [
        "20"
      ],
      "$type": "dimension",
      "$value": "120px"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```

## token/tokens

---
type: token
name: tokens
status: published
---

# Tokens

Resolved avg theme values from themes/default/.

## Token values

| CSS custom property | Value | Notes |
|---|---|---|
| `--font-family-primary` | `'Roboto',
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    Roboto,
    Helvetica,
    Arial,
    sans-serif` | |
| `--font-family-secondary` | `'Lexend',
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    Roboto,
    Helvetica,
    Arial,
    sans-serif` | |
| `--font-weight-light` | `300` | |
| `--font-weight-regular` | `400` | |
| `--font-weight-medium` | `500` | |
| `--font-weight-semibold` | `600` | |
| `--font-weight-bold` | `700` | |
| `--font-weight-extrabold` | `800` | |
| `--letterspacing-h0` | `0` | |
| `--letterspacing-h1` | `0` | |
| `--letterspacing-h2` | `0` | |
| `--letterspacing-h3` | `0` | |
| `--letterspacing-h4` | `0` | |
| `--letterspacing-h5` | `0` | |
| `--letterspacing-h6` | `0` | |
| `--letterspacing-h7` | `0` | |
| `--letterspacing-body-3xl` | `0` | |
| `--letterspacing-body-2xl` | `0` | |
| `--letterspacing-body-xl` | `0` | |
| `--letterspacing-body-lg` | `0` | |
| `--letterspacing-body-base` | `0` | |
| `--letterspacing-body-sm` | `0` | |
| `--letterspacing-body-xs` | `0` | |
| `--letterspacing-button-xl` | `0` | |
| `--letterspacing-button-lg` | `0` | |
| `--letterspacing-button-base` | `0` | |
| `--letterspacing-button-sm` | `0` | |
| `--letterspacing-label` | `0` | |
| `--letterspacing-tagline-lg` | `0` | |
| `--letterspacing-tagline-md` | `0` | |
| `--letterspacing-tagline-sm` | `0` | |
| `--font-size-h0` | `48px` | |
| `--lineheight-h0` | `56px` | |
| `--font-size-h1` | `44px` | |
| `--lineheight-h1` | `52px` | |
| `--font-size-h2` | `36px` | |
| `--lineheight-h2` | `44px` | |
| `--font-size-h3` | `32px` | |
| `--lineheight-h3` | `40px` | |
| `--font-size-h4` | `28px` | |
| `--lineheight-h4` | `34px` | |
| `--font-size-h5` | `24px` | |
| `--lineheight-h5` | `30px` | |
| `--font-size-h6` | `20px` | |
| `--lineheight-h6` | `26px` | |
| `--font-size-h7` | `16px` | |
| `--lineheight-h7` | `22px` | |
| `--font-size-body-3xl` | `22px` | |
| `--lineheight-body-3xl` | `32px` | |
| `--font-size-body-2xl` | `20px` | |
| `--lineheight-body-2xl` | `30px` | |
| `--font-size-body-xl` | `18px` | |
| `--lineheight-body-xl` | `26px` | |
| `--font-size-body-lg` | `16px` | |
| `--lineheight-body-lg` | `24px` | |
| `--font-size-body-base` | `14px` | |
| `--lineheight-body-base` | `22px` | |
| `--font-size-body-sm` | `14px` | |
| `--lineheight-body-sm` | `22px` | |
| `--font-size-body-xs` | `12px` | |
| `--lineheight-body-xs` | `18px` | |
| `--font-size-button-xl` | `18px` | |
| `--lineheight-button-xl` | `24px` | |
| `--font-size-button-lg` | `16px` | |
| `--lineheight-button-lg` | `20px` | |
| `--font-size-button-base` | `14px` | |
| `--lineheight-button-base` | `18px` | |
| `--font-size-button-sm` | `14px` | |
| `--lineheight-button-sm` | `18px` | |
| `--font-size-label` | `12px` | |
| `--lineheight-label` | `16px` | |
| `--font-size-tagline-lg` | `16px` | |
| `--lineheight-tagline-lg` | `20px` | |
| `--font-size-tagline-md` | `14px` | |
| `--lineheight-tagline-md` | `18px` | |
| `--font-size-tagline-sm` | `12px` | |
| `--lineheight-tagline-sm` | `16px` | |
| `--lineheight-h0` | `92px` | |
| `--lineheight-h1` | `72px` | |
| `--lineheight-h2` | `60px` | |
| `--lineheight-h3` | `44px` | |
| `--lineheight-h4` | `36px` | |
| `--lineheight-h5` | `32px` | |
| `--lineheight-h6` | `26px` | |
| `--lineheight-h7` | `22px` | |
| `--lineheight-body-2xl` | `26px` | |
| `--lineheight-body-xl` | `24px` | |
| `--lineheight-body-lg` | `24px` | |
| `--lineheight-body-base` | `22px` | |
| `--lineheight-body-sm` | `20px` | |
| `--font-size-h0` | `72px` | |
| `--lineheight-h0` | `86px` | |
| `--font-size-h1` | `64px` | |
| `--lineheight-h1` | `76px` | |
| `--font-size-h2` | `52px` | |
| `--lineheight-h2` | `62px` | |
| `--font-size-h3` | `44px` | |
| `--lineheight-h3` | `52px` | |
| `--font-size-h4` | `36px` | |
| `--lineheight-h4` | `44px` | |
| `--font-size-h5` | `28px` | |
| `--lineheight-h5` | `34px` | |
| `--font-size-h6` | `24px` | |
| `--lineheight-h6` | `30px` | |
| `--font-size-h7` | `20px` | |
| `--lineheight-h7` | `26px` | |
| `--font-size-body-3xl` | `24px` | |
| `--lineheight-body-3xl` | `36px` | |
| `--font-size-body-2xl` | `22px` | |
| `--lineheight-body-2xl` | `32px` | |
| `--font-size-body-xl` | `20px` | |
| `--lineheight-body-xl` | `30px` | |
| `--font-size-body-lg` | `18px` | |
| `--lineheight-body-lg` | `26px` | |
| `--font-size-body-base` | `16px` | |
| `--lineheight-body-base` | `24px` | |
| `--font-size-body-sm` | `14px` | |
| `--lineheight-body-sm` | `22px` | |
| `--font-size-body-xs` | `12px` | |
| `--lineheight-body-xs` | `18px` | |
| `--font-size-button-xl` | `20px` | |
| `--lineheight-button-xl` | `26px` | |
| `--font-size-button-lg` | `18px` | |
| `--lineheight-button-lg` | `24px` | |
| `--font-size-button-base` | `16px` | |
| `--lineheight-button-base` | `20px` | |
| `--font-size-button-sm` | `14px` | |
| `--lineheight-button-sm` | `18px` | |
| `--font-size-label` | `12px` | |
| `--lineheight-label` | `16px` | |
| `--font-size-tagline-lg` | `18px` | |
| `--lineheight-tagline-lg` | `22px` | |
| `--font-size-tagline-md` | `16px` | |
| `--lineheight-tagline-md` | `20px` | |
| `--font-size-tagline-sm` | `14px` | |
| `--lineheight-tagline-sm` | `16px` | |


### tokens.json

```json
{
  "$schema": "https://design-system.aics.dev/schema/token.v1.json",
  "type": "token",
  "name": "tokens",
  "status": "published",
  "summary": "Resolved avg theme values from themes/default/.",
  "specUrl": "/api/tokens/tokens/spec.md",
  "sourceFiles": {
    "spec": "/api/tokens/tokens/spec.md"
  },
  "figma": null,
  "tokens": [
    {
      "family": "tokens",
      "cssVar": "--font-family-primary",
      "path": [
        "font",
        "family",
        "primary"
      ],
      "$type": "fontFamily",
      "$value": "'Roboto',\n    -apple-system,\n    BlinkMacSystemFont,\n    'Segoe UI',\n    Roboto,\n    Helvetica,\n    Arial,\n    sans-serif"
    },
    {
      "family": "tokens",
      "cssVar": "--font-family-secondary",
      "path": [
        "font",
        "family",
        "secondary"
      ],
      "$type": "fontFamily",
      "$value": "'Lexend',\n    -apple-system,\n    BlinkMacSystemFont,\n    'Segoe UI',\n    Roboto,\n    Helvetica,\n    Arial,\n    sans-serif"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-light",
      "path": [
        "font",
        "weight",
        "light"
      ],
      "$type": "fontWeight",
      "$value": "300"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-regular",
      "path": [
        "font",
        "weight",
        "regular"
      ],
      "$type": "fontWeight",
      "$value": "400"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-medium",
      "path": [
        "font",
        "weight",
        "medium"
      ],
      "$type": "fontWeight",
      "$value": "500"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-semibold",
      "path": [
        "font",
        "weight",
        "semibold"
      ],
      "$type": "fontWeight",
      "$value": "600"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-bold",
      "path": [
        "font",
        "weight",
        "bold"
      ],
      "$type": "fontWeight",
      "$value": "700"
    },
    {
      "family": "tokens",
      "cssVar": "--font-weight-extrabold",
      "path": [
        "font",
        "weight",
        "extrabold"
      ],
      "$type": "fontWeight",
      "$value": "800"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h0",
      "path": [
        "letterspacing",
        "h0"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h1",
      "path": [
        "letterspacing",
        "h1"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h2",
      "path": [
        "letterspacing",
        "h2"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h3",
      "path": [
        "letterspacing",
        "h3"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h4",
      "path": [
        "letterspacing",
        "h4"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h5",
      "path": [
        "letterspacing",
        "h5"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h6",
      "path": [
        "letterspacing",
        "h6"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-h7",
      "path": [
        "letterspacing",
        "h7"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-3xl",
      "path": [
        "letterspacing",
        "body",
        "3xl"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-2xl",
      "path": [
        "letterspacing",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-xl",
      "path": [
        "letterspacing",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-lg",
      "path": [
        "letterspacing",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-base",
      "path": [
        "letterspacing",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-sm",
      "path": [
        "letterspacing",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-body-xs",
      "path": [
        "letterspacing",
        "body",
        "xs"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-button-xl",
      "path": [
        "letterspacing",
        "button",
        "xl"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-button-lg",
      "path": [
        "letterspacing",
        "button",
        "lg"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-button-base",
      "path": [
        "letterspacing",
        "button",
        "base"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-button-sm",
      "path": [
        "letterspacing",
        "button",
        "sm"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-label",
      "path": [
        "letterspacing",
        "label"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-tagline-lg",
      "path": [
        "letterspacing",
        "tagline",
        "lg"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-tagline-md",
      "path": [
        "letterspacing",
        "tagline",
        "md"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--letterspacing-tagline-sm",
      "path": [
        "letterspacing",
        "tagline",
        "sm"
      ],
      "$type": "dimension",
      "$value": "0"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h0",
      "path": [
        "font",
        "size",
        "h0"
      ],
      "$type": "dimension",
      "$value": "48px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h0",
      "path": [
        "lineheight",
        "h0"
      ],
      "$type": "dimension",
      "$value": "56px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h1",
      "path": [
        "font",
        "size",
        "h1"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h1",
      "path": [
        "lineheight",
        "h1"
      ],
      "$type": "dimension",
      "$value": "52px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h2",
      "path": [
        "font",
        "size",
        "h2"
      ],
      "$type": "dimension",
      "$value": "36px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h2",
      "path": [
        "lineheight",
        "h2"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h3",
      "path": [
        "font",
        "size",
        "h3"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h3",
      "path": [
        "lineheight",
        "h3"
      ],
      "$type": "dimension",
      "$value": "40px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h4",
      "path": [
        "font",
        "size",
        "h4"
      ],
      "$type": "dimension",
      "$value": "28px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h4",
      "path": [
        "lineheight",
        "h4"
      ],
      "$type": "dimension",
      "$value": "34px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h5",
      "path": [
        "font",
        "size",
        "h5"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h5",
      "path": [
        "lineheight",
        "h5"
      ],
      "$type": "dimension",
      "$value": "30px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h6",
      "path": [
        "font",
        "size",
        "h6"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h6",
      "path": [
        "lineheight",
        "h6"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h7",
      "path": [
        "font",
        "size",
        "h7"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h7",
      "path": [
        "lineheight",
        "h7"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-3xl",
      "path": [
        "font",
        "size",
        "body",
        "3xl"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-3xl",
      "path": [
        "lineheight",
        "body",
        "3xl"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-2xl",
      "path": [
        "font",
        "size",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-2xl",
      "path": [
        "lineheight",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "30px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-xl",
      "path": [
        "font",
        "size",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-xl",
      "path": [
        "lineheight",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-lg",
      "path": [
        "font",
        "size",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-lg",
      "path": [
        "lineheight",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-base",
      "path": [
        "font",
        "size",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-base",
      "path": [
        "lineheight",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-sm",
      "path": [
        "font",
        "size",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-sm",
      "path": [
        "lineheight",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-xs",
      "path": [
        "font",
        "size",
        "body",
        "xs"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-xs",
      "path": [
        "lineheight",
        "body",
        "xs"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-xl",
      "path": [
        "font",
        "size",
        "button",
        "xl"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-xl",
      "path": [
        "lineheight",
        "button",
        "xl"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-lg",
      "path": [
        "font",
        "size",
        "button",
        "lg"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-lg",
      "path": [
        "lineheight",
        "button",
        "lg"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-base",
      "path": [
        "font",
        "size",
        "button",
        "base"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-base",
      "path": [
        "lineheight",
        "button",
        "base"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-sm",
      "path": [
        "font",
        "size",
        "button",
        "sm"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-sm",
      "path": [
        "lineheight",
        "button",
        "sm"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-label",
      "path": [
        "font",
        "size",
        "label"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-label",
      "path": [
        "lineheight",
        "label"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-lg",
      "path": [
        "font",
        "size",
        "tagline",
        "lg"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-lg",
      "path": [
        "lineheight",
        "tagline",
        "lg"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-md",
      "path": [
        "font",
        "size",
        "tagline",
        "md"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-md",
      "path": [
        "lineheight",
        "tagline",
        "md"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-sm",
      "path": [
        "font",
        "size",
        "tagline",
        "sm"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-sm",
      "path": [
        "lineheight",
        "tagline",
        "sm"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h0",
      "path": [
        "lineheight",
        "h0"
      ],
      "$type": "dimension",
      "$value": "92px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h1",
      "path": [
        "lineheight",
        "h1"
      ],
      "$type": "dimension",
      "$value": "72px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h2",
      "path": [
        "lineheight",
        "h2"
      ],
      "$type": "dimension",
      "$value": "60px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h3",
      "path": [
        "lineheight",
        "h3"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h4",
      "path": [
        "lineheight",
        "h4"
      ],
      "$type": "dimension",
      "$value": "36px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h5",
      "path": [
        "lineheight",
        "h5"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h6",
      "path": [
        "lineheight",
        "h6"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h7",
      "path": [
        "lineheight",
        "h7"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-2xl",
      "path": [
        "lineheight",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-xl",
      "path": [
        "lineheight",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-lg",
      "path": [
        "lineheight",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-base",
      "path": [
        "lineheight",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-sm",
      "path": [
        "lineheight",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h0",
      "path": [
        "font",
        "size",
        "h0"
      ],
      "$type": "dimension",
      "$value": "72px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h0",
      "path": [
        "lineheight",
        "h0"
      ],
      "$type": "dimension",
      "$value": "86px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h1",
      "path": [
        "font",
        "size",
        "h1"
      ],
      "$type": "dimension",
      "$value": "64px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h1",
      "path": [
        "lineheight",
        "h1"
      ],
      "$type": "dimension",
      "$value": "76px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h2",
      "path": [
        "font",
        "size",
        "h2"
      ],
      "$type": "dimension",
      "$value": "52px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h2",
      "path": [
        "lineheight",
        "h2"
      ],
      "$type": "dimension",
      "$value": "62px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h3",
      "path": [
        "font",
        "size",
        "h3"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h3",
      "path": [
        "lineheight",
        "h3"
      ],
      "$type": "dimension",
      "$value": "52px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h4",
      "path": [
        "font",
        "size",
        "h4"
      ],
      "$type": "dimension",
      "$value": "36px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h4",
      "path": [
        "lineheight",
        "h4"
      ],
      "$type": "dimension",
      "$value": "44px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h5",
      "path": [
        "font",
        "size",
        "h5"
      ],
      "$type": "dimension",
      "$value": "28px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h5",
      "path": [
        "lineheight",
        "h5"
      ],
      "$type": "dimension",
      "$value": "34px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h6",
      "path": [
        "font",
        "size",
        "h6"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h6",
      "path": [
        "lineheight",
        "h6"
      ],
      "$type": "dimension",
      "$value": "30px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-h7",
      "path": [
        "font",
        "size",
        "h7"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-h7",
      "path": [
        "lineheight",
        "h7"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-3xl",
      "path": [
        "font",
        "size",
        "body",
        "3xl"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-3xl",
      "path": [
        "lineheight",
        "body",
        "3xl"
      ],
      "$type": "dimension",
      "$value": "36px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-2xl",
      "path": [
        "font",
        "size",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-2xl",
      "path": [
        "lineheight",
        "body",
        "2xl"
      ],
      "$type": "dimension",
      "$value": "32px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-xl",
      "path": [
        "font",
        "size",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-xl",
      "path": [
        "lineheight",
        "body",
        "xl"
      ],
      "$type": "dimension",
      "$value": "30px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-lg",
      "path": [
        "font",
        "size",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-lg",
      "path": [
        "lineheight",
        "body",
        "lg"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-base",
      "path": [
        "font",
        "size",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-base",
      "path": [
        "lineheight",
        "body",
        "base"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-sm",
      "path": [
        "font",
        "size",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-sm",
      "path": [
        "lineheight",
        "body",
        "sm"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-body-xs",
      "path": [
        "font",
        "size",
        "body",
        "xs"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-body-xs",
      "path": [
        "lineheight",
        "body",
        "xs"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-xl",
      "path": [
        "font",
        "size",
        "button",
        "xl"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-xl",
      "path": [
        "lineheight",
        "button",
        "xl"
      ],
      "$type": "dimension",
      "$value": "26px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-lg",
      "path": [
        "font",
        "size",
        "button",
        "lg"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-lg",
      "path": [
        "lineheight",
        "button",
        "lg"
      ],
      "$type": "dimension",
      "$value": "24px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-base",
      "path": [
        "font",
        "size",
        "button",
        "base"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-base",
      "path": [
        "lineheight",
        "button",
        "base"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-button-sm",
      "path": [
        "font",
        "size",
        "button",
        "sm"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-button-sm",
      "path": [
        "lineheight",
        "button",
        "sm"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-label",
      "path": [
        "font",
        "size",
        "label"
      ],
      "$type": "dimension",
      "$value": "12px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-label",
      "path": [
        "lineheight",
        "label"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-lg",
      "path": [
        "font",
        "size",
        "tagline",
        "lg"
      ],
      "$type": "dimension",
      "$value": "18px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-lg",
      "path": [
        "lineheight",
        "tagline",
        "lg"
      ],
      "$type": "dimension",
      "$value": "22px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-md",
      "path": [
        "font",
        "size",
        "tagline",
        "md"
      ],
      "$type": "dimension",
      "$value": "16px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-md",
      "path": [
        "lineheight",
        "tagline",
        "md"
      ],
      "$type": "dimension",
      "$value": "20px"
    },
    {
      "family": "tokens",
      "cssVar": "--font-size-tagline-sm",
      "path": [
        "font",
        "size",
        "tagline",
        "sm"
      ],
      "$type": "dimension",
      "$value": "14px"
    },
    {
      "family": "tokens",
      "cssVar": "--lineheight-tagline-sm",
      "path": [
        "lineheight",
        "tagline",
        "sm"
      ],
      "$type": "dimension",
      "$value": "16px"
    }
  ],
  "generatedAt": "2026-08-03T01:08:42.102Z"
}

```
